---
description: Híbrido PostgreSQL + MongoDB en catalog-service (autoridad y coherencia)
globs:
  - "services/catalog-service/**/*.java"
alwaysApply: false
---

# SQL maestro y Mongo complementario

Documentación detallada: **[docs/data-model/mongo.md](../../docs/data-model/mongo.md)** y reglas de negocio en [data-model.md](../../docs/data-model/data-model.md).

## Estructura de paquetes (JPA + Mongo)

La disposición de paquetes (**`domain`**, **`infrastructure.persistence.jpa.repository`** (+ **`…projection`** / **`…imp`**), **`infrastructure.persistence.mongo.*`**, orquestación en **`application`**) coincide con **## Persistencia** en [spring-boot-4-backend.mdc](spring-boot-4-backend.mdc). Aquí solo **reglas de negocio** del híbrido SQL + Mongo en catálogo (autoridad, colecciones, `_id`, etc.).

## Principios

- **PostgreSQL** es la **autoridad** para **ARBOL**, **ESPECIE**, taxonomía y datos operativos del catálogo que deban ser transaccionales y consultables con integridad relacional.
- **MongoDB** almacena **enriquecimientos** y notas semiestructuradas (`enriquecimientos_especie`, `enriquecimientos_arbol`); **no** sustituye al maestro SQL.
- Toda **denormalización** en documentos Mongo (nombres de especie, etc.) debe tener estrategia de **actualización o invalidación** cuando cambien los datos maestros en SQL.
- Los identificadores lógicos `idArbol` / `idEspecie` en Mongo enlazan con las PK numéricas SQL; mantener **coherencia** tras migraciones o cambios de modelo.
- **`_id` en Mongo:** string con convención **`esp_<idEspecie>`** (una especie) y **`arb_<idArbol>_<ULID>`** (notas de árbol); no usar `ObjectId` por defecto como convención del MVP (detalle en [mongo.md](../../docs/data-model/mongo.md)).
