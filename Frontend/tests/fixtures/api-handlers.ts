/**
 * MSW API handlers for integration tests. Add handlers here to mock
 * backend endpoints (e.g. POST /api/auth/login, GET /api/trips).
 * Install msw and use in tests: setupServer(...handlers) or worker.use(...).
 */

// Placeholder until MSW is added. Example:
// import { http, HttpResponse } from 'msw';
// export const handlers = [
//   http.post('*/api/auth/login', () => HttpResponse.json({ accessToken: 'mock-token', user: mockUser })),
// ];

export const handlers: unknown[] = [];
