import { logger } from '../utils/logger';

export function initializeSchedulers() {
  // TODO: Inicializar cron jobs aquí
  // Ejemplo:
  // cron.schedule('*/30 * * * *', () => {
  //   logger.info('Ejecutando recordatorio de citas...');
  // });
  
  logger.info('Schedulers inicializados (placeholder)');
}
