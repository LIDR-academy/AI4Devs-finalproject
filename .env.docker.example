# Optional overrides for Docker Compose (copy to .env in the repo root).
# Defaults in docker-compose.yml work out of the box for local HTTP access.

JWT_ACCESS_SECRET=mecatrack-docker-access-secret-min-32-chars
JWT_REFRESH_SECRET=mecatrack-docker-refresh-secret-min-32-chars
