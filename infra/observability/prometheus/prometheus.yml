global:
  scrape_interval: 15s
  evaluation_interval: 15s

# Alert rule files are wired in US-O5. Keep this directory mounted for that follow-up.
# rule_files:
#   - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: mecatrack-api
    metrics_path: /api/metrics
    static_configs:
      - targets: ['api:4000']
