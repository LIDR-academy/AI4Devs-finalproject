# Mermaid Diagram Syntax Rules
- **No HTML:** Avoid embedding HTML tags in labels.
- **Escaping:** Escape special characters in quotes (e.g., `node["Label (info)"]`).
- **Orientation:** Prefer `TB` (top-to-bottom) for logic flow and `LR` (left-to-right) for timelines.
