# Tailwind CDN Rules for Prototyping
- **Self-Contained:** Prototypes must load all styles from a public CDN (e.g., `<script src="https://cdn.tailwindcss.com"></script>`).
- **No local files:** Never reference local CSS files.
- **Vanilla JS:** Use embedded `<script>` blocks for interactive prototypes.
