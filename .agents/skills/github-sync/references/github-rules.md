# GitHub Sync Rules & Conventions
- **Labels mapping:** Map local priorities to GitHub labels (e.g., `Priority: High` -> `bug`, `high`).
- **No Secret Leaking:** Never commit personal access tokens. Use environment variables.
- **Bidirectional Sync:** Always pull latest issues state before pushing local changes.
