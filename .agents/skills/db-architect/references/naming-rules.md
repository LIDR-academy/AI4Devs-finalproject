# Database Naming Rules
- **Tables:** lowercase, plural, snake_case (e.g., `user_profiles`).
- **Primary Keys:** `id` (integer or UUID).
- **Foreign Keys:** `singular_table_name_id` (e.g., `user_profile_id`).
- **Columns:** lowercase, snake_case (e.g., `created_at`).
