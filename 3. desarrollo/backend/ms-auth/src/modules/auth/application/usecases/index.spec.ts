/**
 * Archivo índice para tests de use cases
 * Este archivo puede ser usado para configurar mocks compartidos
 */

export const createMockUsuario = () => {
  // Helper para crear usuarios mock en tests
};

export const createMockPerfil = () => {
  // Helper para crear perfiles mock en tests
};

