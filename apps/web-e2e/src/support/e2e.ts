/**
 * Support file for the web acceptance harness.
 *
 * Loaded before every spec. It is deliberately empty of behavior: custom
 * commands, authentication helpers and fixtures arrive with the tickets that
 * own the scenarios needing them (`docs/backlog/C10/test-plan.md`), not with
 * the harness itself.
 */
export {};
