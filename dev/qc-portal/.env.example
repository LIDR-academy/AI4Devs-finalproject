# Dev-only: where `bun run dev` proxies the literal `/streams` path.
# In production the reverse proxy fronts a single origin, so no base URL is baked
# into the built bundle (see openspec design D-P6).
STREAMER_PROXY_TARGET=http://localhost:8080

# Port the production static server (server.ts) listens on.
PORT=3000
