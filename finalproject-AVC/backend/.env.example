# Environment
NODE_ENV=development

# Server
PORT=3000
HOST=0.0.0.0

# Database (Docker PostgreSQL - use docker-compose up -d to start)
DATABASE_URL="postgresql://usuario:password@localhost:5432/sc_padel_db?schema=public"

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:5173
