# Tasks — KAN-30 Selectable read format

## 1. Backend

- [x] 1.1 Allow null `read_format` on PATCH reading-record DTO
- [x] 1.2 Integration tests for set/clear read_format

## 2. Frontend

- [x] 2.1 `ReadFormatSelect` component (accessible)
- [x] 2.2 Book Tracker format column + inline PATCH
- [x] 2.3 CompletionModal uses `ReadFormatSelect`

## 3. Verification

- [x] 3.1 `MANUAL-TEST-KAN-30.md`; build + integration tests
