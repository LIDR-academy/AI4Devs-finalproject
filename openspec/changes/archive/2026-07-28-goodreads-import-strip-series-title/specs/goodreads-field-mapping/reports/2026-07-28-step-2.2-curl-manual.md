# Step 2.2 Report - Manual curl Import Verification

- Date: 2026-07-28
- Change: goodreads-import-strip-series-title
- Agent: Cursor Auto

## Environment

- Temporary Nest server: `PORT=3011 npm run start:dev` (feature branch code)
- Auth: `POST /v1/auth/dev-login` with `strip-series-test@example.com`
- CSV fixture: single Goodreads row, Title `The Raven Scholar (Eternal Path Trilogy, #1)`

## Commands

```bash
curl -s -X POST http://localhost:3011/v1/auth/dev-login \
  -H 'Content-Type: application/json' \
  -d '{"email":"strip-series-test@example.com"}'

curl -s -X POST http://localhost:3011/v1/import/goodreads \
  -H "Authorization: Bearer $TOKEN" \
  -F "file=@/tmp/gr-raven-test.csv"

curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:3011/v1/import/jobs/$JOB_ID"

curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:3011/v1/books"
```

## Results

- Job status: `completed`, `imported_count: 1`
- `mapped_rows[0].book.title`: `The Raven Scholar`
- `mapped_rows[0].book.series_name`: `Eternal Path Trilogy`
- `GET /v1/books` effective fields: `title=The Raven Scholar`, `series_name=Eternal Path Trilogy`
- `enrichment_failed_count: 0`

## Database Restoration

- Deleted reading_records, book, catalog_edition (`external_provider_id=88001`), import_jobs, and user `strip-series-test@example.com` from local Postgres
- Temporary server on port 3011 stopped after verification
