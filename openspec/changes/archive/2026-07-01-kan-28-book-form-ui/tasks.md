# Tasks — KAN-28 Book form UI

## 1. Form logic

- [x] 1.1 `frontend/src/lib/bookForm.ts` — state, validation, payloads
- [x] 1.2 `BookFormModal` full fields + save (create + edit)

## 2. Integration

- [x] 2.1 `BookTrackerPage` — `onSaved`, completion modal from form save

## 3. Verification

- [x] 3.1 `MANUAL-TEST-KAN-28.md`; `npm run build` frontend
