# Tasks — KAN-22 Book Tracker page restyle

## 1. Page shell

- [x] 1.1 Replace header with `PageHeader` + `Button` in BookTrackerPage
- [x] 1.2 Migrate page container CSS to design tokens

## 2. Responsive table

- [x] 2.1 Wrap table with `TableScroll` and `Table` from `components/ui/`
- [x] 2.2 Add column width/alignment classes on headers and row cells
- [x] 2.3 Remove hard-coded hex from BookTrackerPage.css

## 3. Verification

- [x] 3.1 `MANUAL-TEST-KAN-22.md`; `npm run build` and `npm run lint` in frontend
