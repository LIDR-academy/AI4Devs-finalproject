# Tasks — KAN-27 Book PATCH API

## 1. Backend

- [x] 1.1 Expand `PatchBookDto` with all book metadata fields
- [x] 1.2 `BooksService.update()` partial apply + empty-body guard
- [x] 1.3 Integration tests (manual POST, full PATCH, validation, 404)

## 2. Docs and types

- [x] 2.1 Update `docs/api-spec.yml` `PatchBookRequest`
- [x] 2.2 Align `frontend/src/api/types.ts` `PatchBookPayload`

## 3. Verification

- [x] 3.1 `MANUAL-TEST-KAN-27.md`; integration tests + backend build
