# Tasks — KAN-26 Manual book edit entry point

## 1. Frontend — entry point

- [x] 1.1 `BookFormModal` shell (`ui/Modal`, edit mode, disabled Save, placeholder body)
- [x] 1.2 Actions column + pencil button on `BookTrackerRow` with accessible label
- [x] 1.3 `BookTrackerPage` state: open modal with selected book; close handler

## 2. Verification

- [x] 2.1 `MANUAL-TEST-KAN-26.md`; `npm run build` frontend
