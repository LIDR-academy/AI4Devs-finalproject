---
id: T-C10-07
epic: C10
story: —
foundation: true
requirements: []
shape: greenfield
platform: shared
layer: util (libs/shared/util)
agent: backend-engineer
estimate: 1h
phase: 0
---

# [C10] T-C10-07 · `libs/shared/util` — pure helper library

## Context

Foundation work with no user story behind it. Source: `docs/backlog/epic-map.md` → **Foundation ownership (priced once)**, row *`libs/shared/contracts`, `libs/shared/util`*, priced in `C10`; and finding **F14** of `user-stories.md`.

`type:util` is the only library type every other type may depend on (`ARCHITECTURE.md` §5.3), so it is created first among the shared libraries and must stay framework-free.

## Scope

- `pnpm nx g @nx/js:lib shared-util --directory=libs/shared/util --tags=platform:shared,scope:shared,type:util` (`ARCHITECTURE.md` §5.5).
- Barrel `index.ts` exporting the minimal helper set this epic actually needs: a result/either helper, an exhaustiveness assertion, a non-empty-string guard.
- Jest configured for the library.
- Out of scope: anything carrying a dependency. `type:util` may depend on nothing but `type:util`.

## Acceptance criteria

Mechanical checks — this ticket has no user scenario.

**Given** the library **When** `pnpm nx test shared-util` runs **Then** it passes.

**Given** the library source **When** it is inspected **Then** it imports no framework, no Angular, no NestJS, no TypeORM and no Node I/O module.

**Given** a probe import into `libs/shared/util` from any other library **When** lint runs **Then** it fails; the probe is reverted before the ticket is closed.

**Given** the project **When** its tags are inspected **Then** they are exactly `platform:shared`, `scope:shared`, `type:util`.
