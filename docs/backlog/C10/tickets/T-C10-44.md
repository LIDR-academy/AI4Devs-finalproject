---
id: T-C10-44
epic: C10
story: US-C10-07
requirements: [FR-IAM-03]
shape: greenfield
platform: backend
layer: infrastructure (identity-access)
agent: backend-engineer
estimate: 2.5h
phase: 0
---

# [C10] T-C10-44 · TypeORM persistence and migration for scope grants

## Context

The persisted half of `T-C10-43`: `US-C10-07` requires the grant to be persisted and explicit, and `US-C10-08` requires a league grant to resolve to its competition set **at evaluation time**, so a competition added to a league is included without any change to the actor grants.

## Scope

- TypeORM entity, mapper and `TypeOrmScopeGrantRepository` in `libs/identity-access/infrastructure`.
- Migration creating `identity_access.scope_grant`: subject user, kind, target identifier, unique on the triple, indexed by subject.
- The league-to-competition resolution implemented as a query executed per evaluation, not as a materialized snapshot on the grant.
- Integration tests against real PostgreSQL, including one that adds a competition to a league between two evaluations.
- Out of scope: the predicates; any competition or league table, which this epic does not own.

## Acceptance criteria

**Given** the migration **When** it runs and is reverted **Then** `identity_access.scope_grant` is created and removed cleanly.

**Given** the same grant inserted twice **When** the second insert executes **Then** the database rejects it on the unique constraint.

**Given** a league grant **When** a competition is added to that league and the grant is resolved again **Then** the new competition appears in the resolved set with no write to the grant row.

**Given** the repository **When** it returns grants **Then** the caller receives domain `ScopeGrant` objects, never TypeORM entities.
