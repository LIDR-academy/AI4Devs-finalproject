# US-004 — Limpiar filtros activos

## Estado del workflow

- [x] Fase 1 — US refinada y backlog generado
- [ ] Fase 2 — Backend implementado (TDD)
- [x] Fase 3 — Frontend implementado (TDD)
- [x] Fase 4 — Verificación (tests en verde)
- [x] Fase 5 — Revisión de seguridad aprobada (sin HIGH/CRITICAL)
- [x] Fase 6 — Cierre (checklist completado)

---

## US refinada

**Historia de usuario:**
Como corredor, quiero ver cuántos filtros tengo activos y poder eliminarlos todos de una vez, para recuperar el catálogo completo rápidamente sin tener que desmarcar cada filtro individualmente.

**Caso de uso asociado:** CU1 — Búsqueda filtrada de productos para running

**Descripción:**
Mecanismo de retroalimentación y control del estado de filtrado. Informa al corredor de cuántos filtros tiene activos y le permite resetear la búsqueda con una única acción. Historia de soporte a US-002 y US-003 que mejora la usabilidad del panel de filtros.

**Criterios de aceptación:**
- [x] Escenario principal: cuando hay filtros activos, se muestra un badge con el número total de filtros activos y un botón de limpieza global; al pulsarlo, todos los filtros se resetean y el catálogo muestra el listado completo
- [x] El botón de limpieza global solo es visible cuando hay al menos un filtro activo
- [x] Al limpiar, el badge desaparece y el contador de resultados refleja el total del catálogo
- [x] Escenario alternativo: el corredor elimina filtros uno a uno desde el panel; el badge se decrementa en consecuencia
- [x] Error/validación: limpiar filtros no recarga la página ni modifica la posición de scroll del corredor en el catálogo

**Datos o entidades implicadas:**
- Estado local de filtros activos en el componente `FilterPanel`

**Estimación:** S

**Prioridad:** Importante

### Ambigüedades resueltas

| Punto ambiguo | Decisión |
|---|---|
| **Texto del botón: «Limpiar filtros» (título de la US) vs. «Limpiar» (`docs/DESIGN-SYSTEM.md`, sección "FilterPanel (cabecera)")** | Se usa el texto del Design System: **"Limpiar"** en la cabecera del `FilterPanel`. `DESIGN-SYSTEM.md` es la fuente de verdad visual obligatoria según `CLAUDE.md` ("Obligatorio antes de implementar cualquier componente... Es la fuente de verdad visual"). Es un control **distinto** del botón "Limpiar filtros" que ya existe en `FilterEmptyState` (alcance cerrado de US-002); ese botón no se modifica. |
| **¿Qué cuenta como "filtro activo" para el badge?** | Ya implementado en US-002/US-003 y sin cambios: `distance.length + surface.length + level.length + objective.length + (category ? 1 : 0) + (priceMaxParam ? 1 : 0)`, calculado en `FilterPanel` a partir de `useSearchParams()`. |
| **Escenario alternativo (quitar filtros uno a uno decrementa el badge)** | Ya es el comportamiento existente: `activeCount` se recalcula en cada render a partir de la URL, y `handleToggle`/`handleCategoryChange`/`handlePriceChange` (US-002/US-003) ya actualizan la URL al desmarcar un filtro individual. No requiere código de producción nuevo — se cubre con un test de regresión explícito para esta US (criterio de hecho de US-004, no solo de las US anteriores). |
| **"No recarga la página ni modifica el scroll"** | Se reutiliza exactamente el patrón ya usado por `handleToggle`/`handleCategoryChange`/`handlePriceChange`: `router.replace(..., { scroll: false })` + `router.refresh()` dentro de `startTransition`. Nunca `window.location`, `router.push` ni un `<a>`/formulario que provoque navegación completa. |
| **Estructura HTML de la cabecera del `FilterPanel`** | Hoy toda la fila de cabecera (icono + título + badge + chevron) es un único `<button>` (toggle de colapso en mobile). Añadir el botón "Limpiar" dentro de ese `<button>` produciría un `<button>` anidado dentro de otro `<button>` (HTML inválido / problema de accesibilidad). Se reestructura a un `<div className="flex items-center justify-between ...">` (clase exacta de `DESIGN-SYSTEM.md`) que contiene dos hermanos: el `<button>` de colapso (icono + título + badge + chevron, sin cambios de comportamiento) y el nuevo `<button>` "Limpiar" condicional. |
| **Alcance del "limpiar todo"** | Elimina **todos** los parámetros de filtro de la URL (`distance`, `surface`, `level`, `objective`, `category`, `priceMax`), no solo los visibles en la sección abierta del panel. Implementación más simple y robusta: `router.replace('/', { scroll: false })` en lugar de reconstruir un `URLSearchParams` filtrado. |

### Contrato técnico — Frontend (componente)

**Componente afectado:** `frontend/src/components/catalog/filter-panel.tsx` (Client Component existente, se extiende; sin cambios de props)

```
Props: activeFilters: ProductFilters   (sin cambios)
Estado: sin estado nuevo; reutiliza `activeCount` ya calculado desde useSearchParams()

Nuevo handler:
  const handleClearAll = () => {
    startTransition(() => {
      router.replace('/', { scroll: false });
      router.refresh();
    });
  };

Cabecera (reestructurada, sin endpoint ni dato nuevo):
  <div className="flex w-full items-center justify-between gap-2 lg:mb-6">
    <button ...colapso mobile, icono Filter + título + badge + chevron... />
    {activeCount > 0 && (
      <button type="button" onClick={handleClearAll} className="text-sm text-blue-600 hover:text-blue-700">
        Limpiar
      </button>
    )}
  </div>
```

No hay endpoint de API nuevo (no se toca el backend); no hay estados de loading/empty/error nuevos — el resultado de limpiar (catálogo completo o `CatalogEmptyState`) ya está cubierto por `page.tsx`/`ProductGrid`/`CatalogEmptyState` existentes (US-001/US-002). No hay parámetros de URL nuevos que validar (`CLAUDE.md` — sanitización de filtros): el handler solo **elimina** parámetros existentes, no introduce valores nuevos.

---

## Tabla de tareas

| ID | Capa | Tarea | Depende de | Verificacion |
|---|---|---|---|---|
| US-004-TASK-01 | Frontend | Añadir botón "Limpiar" a la cabecera del `FilterPanel`, visible solo con `activeCount > 0`, que resetea todos los filtros sin recargar la página ni alterar el scroll | — | RTL: `filter-panel.test.tsx` — botón ausente sin filtros activos; presente con ≥1 filtro activo; `onClick` llama `router.replace('/', { scroll: false })` y `router.refresh()` |
| US-004-TASK-02 | Frontend | Test de regresión: el badge decrementa al quitar un filtro individual y desaparece cuando no quedan filtros activos | US-004-TASK-01 | RTL: `filter-panel.test.tsx` — desmarcar un filtro de varios activos deja el resto en la URL resultante; con `searchParams` vacíos el badge (`data-testid="filter-badge"`) no se renderiza |

---

## Detalle de tareas

---

### `US-004-TASK-01` — Botón "Limpiar" en la cabecera del `FilterPanel`

- [x] Implementado
- **Capa:** Frontend
- **Depende de:** —
- **Criterio de aceptación cubierto:** "Escenario principal: cuando hay filtros activos, se muestra un badge... y un botón... al pulsarlo, todos los filtros se resetean y el catálogo muestra el listado completo"; "El botón... solo es visible cuando hay al menos un filtro activo"; "Error/validación: limpiar filtros no recarga la página ni modifica la posición de scroll"

**Descripción técnica**

En `frontend/src/components/catalog/filter-panel.tsx`:

1. Se añade `handleClearAll`, que llama `router.replace('/', { scroll: false })` + `router.refresh()` dentro de `startTransition` (mismo patrón que los handlers existentes).
2. Se reestructura la cabecera: el `<button>` de colapso pasa a envolver únicamente icono + título + badge + chevron; se añade un `<div className="flex w-full items-center justify-between gap-2 lg:mb-6">` como contenedor de ese botón y del nuevo botón "Limpiar" (clases exactas de `DESIGN-SYSTEM.md`: `text-sm text-blue-600 hover:text-blue-700`).
3. El botón "Limpiar" se renderiza condicionalmente: `{activeCount > 0 && (...)}`.

**Criterios de hecho**

- Sin filtros activos (`activeCount === 0`), el botón "Limpiar" no está en el DOM.
- Con al menos un filtro activo, el botón "Limpiar" es visible junto al badge, en la misma fila de cabecera.
- Al pulsar "Limpiar", se llama `router.replace('/', { scroll: false })` — nunca `router.push` ni navegación de página completa.
- Al pulsar "Limpiar", se llama `router.refresh()` para forzar el re-render del Server Component (mismo patrón que el resto de handlers de filtro).
- El botón de colapso mobile (icono + título + badge + chevron) conserva su comportamiento actual sin regresión.
- Cumple las reglas de seguridad aplicables de `CLAUDE.md` (no se introducen parámetros de URL nuevos; no hay `dangerouslySetInnerHTML`).

**Tests TDD (obligatorios)**

> Primero el test en rojo, luego el código mínimo, luego refactor en verde.

| Test | Tipo | Aserción |
|---|---|---|
| `FilterPanel no renderiza el botón Limpiar sin filtros activos` | RTL | `screen.queryByRole('button', { name: 'Limpiar' })` es `null` con `searchParams` vacíos |
| `FilterPanel renderiza el botón Limpiar con al menos un filtro activo` | RTL | botón presente cuando `searchParams` tiene `distance=5K` |
| `click en Limpiar llama router.replace con '/' y scroll:false` | RTL + mock router | `router.replace` llamado con `('/', { scroll: false })` |
| `click en Limpiar llama router.refresh tras router.replace` | RTL + mock router | `router.refresh` invocado después de `router.replace` |
| `click en Limpiar elimina todos los params existentes, no solo uno` | RTL + mock router | con `searchParams` `distance=5K&category=shoes&priceMax=100`, `router.replace` se llama con exactamente `'/'` (sin query string) |
| `el botón de colapso mobile sigue funcionando tras la reestructuración` | RTL | clic en el botón de colapso sigue alternando `aria-expanded` / la visibilidad de `#filter-sections` |

- Test en rojo escrito antes del código de producción: sí.

**Verificación ejecutada**

```
Rojo (antes de implementar) — filter-panel.test.tsx:
  ✓ does not render the Limpiar button when no filters are active
  × renders the Limpiar button when at least one filter is active
  × calls router.replace with "/" and scroll:false when Limpiar is clicked
  × calls router.refresh after router.replace when Limpiar is clicked
  × clears all active params, not just one, when Limpiar is clicked
  ✓ still allows toggling the mobile collapse button after the header restructuring

Verde (tras implementar):
$ npx vitest run src/components/catalog/filter-panel.test.tsx
 ✓ src/components/catalog/filter-panel.test.tsx (41 tests) 206ms
 Test Files  1 passed (1)
      Tests  41 passed (41)

$ npm test -- --run
 Test Files  30 passed (30)
      Tests  298 passed (298)

$ npx tsc --noEmit
(sin salida — 0 errores)

Verificación manual en navegador (Playwright, stack real: Postgres + backend npm run dev
+ frontend npm run dev, /?distance=5K&category=shoes):
  - Layout desktop y mobile: botón "Limpiar" visible junto al badge "2", sin solaparse
    con el botón de colapso (capturas de pantalla revisadas).
  - Click en "Limpiar" → URL pasa a "/", badge desaparece (0 elementos con
    data-testid="filter-badge"), grid muestra el catálogo completo.
  - Posición de scroll preservada tras "Limpiar" (confirmado con dispatchEvent de click
    nativo, sin la interferencia del scroll-into-view automático del driver de test).
  - Toggle de colapso mobile (clase hidden/block de #filter-sections) sigue funcionando
    tras la reestructuración del header — sin regresión.
```

---

### `US-004-TASK-02` — Regresión: decremento del badge al quitar filtros individuales

- [x] Implementado
- **Capa:** Frontend
- **Depende de:** US-004-TASK-01
- **Criterio de aceptación cubierto:** "Al limpiar, el badge desaparece y el contador de resultados refleja el total del catálogo"; "Escenario alternativo: el corredor elimina filtros uno a uno desde el panel; el badge se decrementa en consecuencia"

**Descripción técnica**

No se modifica código de producción: `activeCount` ya se recalcula desde `useSearchParams()` en cada render, y `handleToggle`/`handleCategoryChange`/`handlePriceChange` (US-002/US-003) ya reescriben la URL al desmarcar un filtro. Esta tarea añade tests de regresión a `frontend/src/components/catalog/filter-panel.test.tsx` que verifican explícitamente, para US-004, que:
- desmarcar un filtro de varios activos preserva el resto en la URL resultante (decremento implícito del badge en el siguiente render).
- con `searchParams` vacíos (estado tras "Limpiar" o tras desmarcar el último filtro), el badge no se renderiza.

**Criterios de hecho**

- Con dos filtros activos (`distance=5K&surface=road`), desmarcar el checkbox de `distance` llama `router.replace` con una URL que contiene `surface=road` pero no `distance=5K`.
- Con `searchParams` vacíos, `screen.queryByTestId('filter-badge')` es `null`.
- Cumple las reglas de seguridad aplicables de `CLAUDE.md`.

**Tests TDD (obligatorios)**

| Test | Tipo | Aserción |
|---|---|---|
| `quitar un filtro de varios activos preserva el resto en la URL resultante` | RTL + mock router | con `searchParams` `distance=5K&surface=road`, desmarcar `distance` llama `router.replace` con URL que contiene `surface=road` y no contiene `distance=5K` |
| `el badge no se renderiza con searchParams vacíos` | RTL | `searchParams` vacíos → `screen.queryByTestId('filter-badge')` es `null` |

- Test en rojo escrito antes del código de producción: **no aplica** — el comportamiento ya existe desde US-002/US-003 (`activeCount` derivado de la URL, handlers de toggle ya implementados). Estos tests son de regresión que confirman explícitamente el criterio de aceptación de US-004; no se espera que fallen contra el código actual, y no se modifica producción en esta tarea. Si alguno fallara inesperadamente, se trataría como un bug a corregir antes de cerrar la US.

**Verificación ejecutada**

```
$ npx vitest run src/components/catalog/filter-panel.test.tsx
 ✓ src/components/catalog/filter-panel.test.tsx (43 tests) 188ms
 Test Files  1 passed (1)
      Tests  43 passed (43)

Los 2 tests nuevos pasaron en verde de inmediato (sin código de producción
nuevo), confirmando que el comportamiento ya estaba implementado por
US-002/US-003:
  ✓ removing one filter from several active ones preserves the rest in the
    resulting URL
  ✓ does not render the badge when there are no active filters left

$ npm test -- --run (suite completa frontend)
 Test Files  30 passed (30)
      Tests  300 passed (300)

$ npx tsc --noEmit
(sin salida — 0 errores)
```

---

## Verificación integrada

Una vez completas las 2 tareas:

1. **E2E (manual o Playwright):** acceder a `/?distance=5K&category=shoes`, comprobar que el badge muestra "2" y el botón "Limpiar" es visible en la cabecera del panel → pulsar "Limpiar" → la URL vuelve a `/`, el badge desaparece, el grid muestra el catálogo completo y `ResultsCounter` refleja el total sin filtrar.
2. **E2E:** con un único filtro activo (`?distance=5K`), desmarcar el checkbox correspondiente directamente (sin usar "Limpiar") → la URL vuelve a `/`, el badge desaparece y el botón "Limpiar" deja de ser visible.
3. **E2E:** con tres filtros activos, quitar uno (checkbox) → el badge pasa de "3" a "2" → pulsar "Limpiar" → el badge desaparece por completo.
4. **Manual:** hacer scroll hacia abajo en el catálogo con filtros activos, pulsar "Limpiar" → la posición de scroll no cambia y la página no recarga (sin parpadeo de navegación completa).

Todos los tests unitarios deben pasar con `npm test` en frontend antes de pasar a la Fase 4. No hay cambios en backend; no aplica `npm test` de backend para esta US.

### Resultado de la Fase 4

```
Frontend: npm test -- --run → 30 suites, 300/300 passed
Frontend: npx tsc --noEmit → 0 errores
Backend:  npm test → 17 suites, 169/169 passed (sin cambios introducidos por esta US;
          confirma que no hay regresión cruzada)
```

**Smoke test manual contra stack real** (PostgreSQL en Docker + `backend npm run dev` + `frontend npm run dev`, verificado con Playwright en desktop 1280px y mobile 390px):

| Escenario | Resultado |
|---|---|
| `/?distance=5K&category=shoes` | Badge muestra "2"; botón "Limpiar" visible junto al badge, sin solaparse con el toggle de colapso (desktop y mobile) |
| Click en "Limpiar" | URL pasa a `/`; badge desaparece (`filter-badge` ausente); grid muestra los 12 productos del catálogo completo |
| Posición de scroll tras "Limpiar" | Preservada (confirmado disparando el evento de click de forma nativa, sin la interferencia del scroll-into-view automático del driver de pruebas); sin recarga completa de página |
| Toggle de colapso mobile tras la reestructuración del header | Sigue alternando `aria-expanded` y la clase `hidden`/`block` de `#filter-sections` sin regresión |

Confirmado: badge + botón "Limpiar" en cabecera, visibilidad condicional, reseteo completo de filtros, sin recarga ni cambio de scroll, y decremento del badge al quitar filtros uno a uno (comportamiento ya cubierto por US-002/US-003, confirmado explícitamente en TASK-02).

---

## OWASP — Revisión de seguridad

**Alcance revisado:** cambio frontend-only, sin commits (working tree de la rama
`us/US-004-limpiar-filtros-activos` sobre base `feature-entrega2-XVB`). Confirmado con
`git diff feature-entrega2-XVB...HEAD` (vacío — no hay commits nuevos) y
`git status --porcelain`: los únicos ficheros modificados son exactamente
`frontend/src/components/catalog/filter-panel.tsx` y
`frontend/src/components/catalog/filter-panel.test.tsx` (más el propio
`docs/backlog/US-004.md`, sin tocar producción). Sin cambios en
`package.json`/`package-lock.json` (sin nuevas dependencias), sin nuevos endpoints,
sin acceso a `sessionId`/carrito/pedidos, sin PII.

### Superficie de ataque de US-004 (anticipada)

| Vector | Riesgo inicial | Mitigación prevista |
|---|---|---|
| Manipulación de parámetros de URL | Ninguno nuevo | El handler `handleClearAll` solo **elimina** parámetros existentes (`router.replace('/')`); no construye ni acepta ningún valor nuevo proveniente del usuario |
| XSS via contenido dinámico | Ninguno | El botón "Limpiar" no renderiza ningún dato dinámico ni de usuario; texto estático, sin `dangerouslySetInnerHTML` |
| DoS / recarga completa de página | Ninguno | `router.replace` (Next.js client-side navigation) en vez de `window.location` o `<a href>`; no genera una petición de documento completo |

No se anticipan hallazgos HIGH/CRITICAL: esta US no introduce endpoints, no toca `sessionId`/carrito/pedidos, no acepta datos nuevos del usuario y no renderiza contenido no confiable.

### Hallazgos

| ID | Severidad | Componente | Vector / Exploit | Fix | Estado |
|---|---|---|---|---|---|
| SEC-04-01 | N/A | — | **Broken access control / IDOR sobre `sessionId`** — esta US no toca carrito, pedidos ni ningún endpoint con `sessionId`; no hay backend involucrado en el diff. | No aplica fix. | N/A — confirmado, no abre superficie nueva |
| SEC-04-02 | N/A | — | **Injection** — sin Prisma, sin SQL, sin backend tocado en este diff. | No aplica fix. | N/A |
| SEC-04-03 | LOW (aceptado) | `filter-panel.tsx` (`handleClearAll`) | **XSS / manipulación de parámetros** — `handleClearAll` llama `router.replace('/', { scroll: false })` con un literal de cadena estático (`'/'`); nunca interpola `searchParams.toString()`, `window.location` ni ningún valor leído del usuario. No construye una URL a partir de input — la operación es puramente "ir a la ruta raíz sin query string". El texto del botón ("Limpiar") es estático en JSX, sin `dangerouslySetInnerHTML`. Verificado leyendo el handler completo y confirmando que no hay una rama de código que reconstruya parámetros a partir de un valor no validado. | Ninguno requerido — no hay superficie de inyección porque no hay interpolación de datos externos en la navegación. | Cerrado — sin acción |
| SEC-04-04 | LOW (aceptado) | `filter-panel.tsx` (reestructuración de la cabecera) | **Seguridad de UI / clickjacking interno (defensa en profundidad)** — la cabecera pasó de un único `<button>` (icono+título+badge+chevron) a un `<div>` contenedor con dos `<button>` hermanos (el de colapso y el nuevo "Limpiar"), evitando el anti-patrón de un `<button>` anidado dentro de otro `<button>` (HTML inválido que en algunos navegadores produce comportamiento de clic ambiguo/inesperado). La reestructuración es estrictamente una mejora, no una regresión: ambos botones son `type="button"` explícito (no `submit`, no disparan envío de formulario), no se solapan visualmente (`flex justify-between gap-2`, confirmado en verificación manual desktop/mobile) y cada uno tiene su propio área de clic delimitada por el DOM, sin superposición de capas (`z-index`/`position: absolute`) que pudiera permitir un clic interceptado. Test de regresión (`still allows toggling the mobile collapse button after the header restructuring`) confirma que el botón de colapso sigue funcionando de forma aislada tras el cambio. | Ninguno requerido. | Cerrado — sin acción |
| SEC-04-05 | N/A | — | **CSRF** — no hay petición de mutación al servidor en este diff; `router.replace`/`router.refresh` son navegación/render client-side de Next.js, no llamadas de red mutables. | No aplica fix. | N/A |
| SEC-04-06 | N/A | — | **Security misconfiguration (CORS/rate-limiting/error-handler)** — sin cambios en backend, `cors.ts`, `rate-limit.ts` ni `error-handler.ts`. | No aplica fix. | N/A |
| SEC-04-07 | N/A | — | **Secretos / PII / logging** — el diff no toca `CartContext`, `localStorage`, `PaymentData` ni ningún logger; no se introduce ningún campo nuevo de datos. | No aplica fix. | N/A |
| SEC-04-08 | LOW (informativo) | `frontend` (dependencias, preexistente) | **Dependencias vulnerables** — `npm audit --audit-level=high` en `frontend/` reporta 3 hallazgos, ninguno HIGH/CRITICAL: 1 LOW (`esbuild` ≤0.28.0, lectura arbitraria de fichero en el dev server de Windows) y 2 MODERATE (`postcss` <8.5.10 vía `next`, XSS en stringify de CSS — mitigación requeriría downgrade breaking a `next@9.x`, fuera de alcance). Confirmado que no son introducidos por esta US: el diff no toca `package.json` ni `package-lock.json` (verificado con `git diff --name-only`). | Ninguno en el alcance de US-004 — deuda técnica preexistente de tooling/build, no de runtime de producción ni relacionada con el componente `FilterPanel`. | Aceptado — preexistente, no bloqueante |

### Resultado de la revisión

**Cero hallazgos HIGH/CRITICAL.** El alcance verificado del diff (`filter-panel.tsx` +
`filter-panel.test.tsx`, sin commits, working tree únicamente) confirma que la mayoría
de vectores OWASP son N/A por diseño: esta US no toca backend, `sessionId`, carrito,
pedidos, PII, ni introduce un endpoint nuevo. El único código de producción nuevo
(`handleClearAll`) navega con un literal estático (`'/'`) sin interpolar ningún dato de
usuario, reutilizando exactamente el patrón `router.replace(..., { scroll: false }) +
router.refresh()` ya auditado en US-002/US-003. La reestructuración de la cabecera
(de un `<button>` anidando contenido a un `<div>` con dos `<button>` hermanos) es una
mejora de corrección HTML/accesibilidad sin introducir superposición de capas ni
comportamiento de clic ambiguo, confirmada con test de regresión. `npm audit` no
reporta HIGH/CRITICAL y no hay cambios de dependencias en este diff.

**Revisión de seguridad aprobada.**

### Remediaciones aplicadas

Ninguna remediación de código fue necesaria — no se encontraron HIGH/CRITICAL. Sin
mejoras adicionales pendientes para esta US.

---

## Revisión de arquitectura limpia (`code-review`)

Ejecutada sobre el diff completo (`filter-panel.tsx` + `filter-panel.test.tsx`, working
tree de `us/US-004-limpiar-filtros-activos` sobre `feature-entrega2-XVB`; confirmado con
`git diff --stat feature-entrega2-XVB` — exactamente esos dos ficheros de aplicación,
sin `package.json`/`package-lock.json`).

| Sección | Veredicto |
|---|---|
| Arquitectura limpia | Pass — N/A backend; Client Component existente sin cambio de capa; export nombrado (`export function FilterPanel`), sin `export default`; test co-localizado (`filter-panel.test.tsx`) |
| Checklist Frontend | Pass — estados loading/empty/error reutilizados sin cambios; sin datos de tarjeta/localStorage tocados; `handleClearAll` solo elimina parámetros de URL existentes (no introduce ni valida valores nuevos, por lo que la regla de sanitización de filtros no aplica a código nuevo); sin `dangerouslySetInnerHTML`; `'use client'` ya requerido, sin cambio; `interface FilterPanelProps` existente, sin `React.FC`; tests RTL nombrados y en verde (43/43) |
| Transversal | Pass — las 2 tareas mapean a los 5 criterios de aceptación; suite completa en verde pegada en el backlog (frontend 300/300, backend 169/169 sin regresión); sin scope creep; sin commits aún (ninguno solicitado) |

**Veredicto: sin hallazgos.** Aprobado sin acciones requeridas.

---

## Cierre — US-004

- [x] Todas las tareas del backlog marcadas `- [x] Implementado`
- [x] Cada criterio de aceptación de la US mapeado y verificado
- [x] Suite completa en verde (frontend 300/300; backend 169/169 sin regresión; sin E2E — fuera de alcance de esta US)
- [x] Revisión de arquitectura limpia superada (skill `code-review`)
- [x] Revisión de seguridad aprobada (sin HIGH/CRITICAL abiertos)
- [x] Reglas de seguridad de `CLAUDE.md` verificadas
- [x] Estado del workflow: fases 1-5 marcadas
- [x] Sin alcance fuera de la US

---

## Pull Request

https://github.com/xavierventeo/AI4Devs-finalproject/pull/14
