---
ticket: TK-074
title: Backend — Storage Locations Management API
epic: Inventario y Bodega
user_story: US-016
status: BACKLOG
---

# 🎟️ Ticket Técnico: TK-074 — Backend Storage Locations API

## 🎯 Objetivo
Implementar la API CRUD de sectores de almacenamiento (`StorageLocation`), repositorio Prisma `PrismaLocationRepository.ts`, casos de uso (`GetLocationsUseCase`, `CreateLocationUseCase`) y endpoints HTTP `/api/v1/locations`.

---

## 🛠️ Tareas Técnicas
1. Implementar `StorageLocation` domain entity.
2. Crear `PrismaLocationRepository.ts` e `InMemoryLocationRepository.ts`.
3. Implementar controlador `locations.controller.ts` con Zod validation.
4. Poblar sectores iniciales por defecto en `prisma/seed.ts` (*Bodega Secos*, *Cámara de Congelados*, *Refrigerador Principal*, *Mesa de Preparación*).

---

## 🧪 Plan de Pruebas
- Pruebas unitarias para `CreateLocationUseCase`.
- Pruebas de integración HTTP `GET /api/v1/locations` y `POST /api/v1/locations`.
