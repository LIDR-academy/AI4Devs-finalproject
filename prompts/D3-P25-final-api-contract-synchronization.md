# D3-P25 - Final API Contract Synchronization

Author: SCDF  
Delivery: Delivery 3 - Final Functional Product  
Prompt language: English  

## Prompt

```text
Synchronize the final ComercIA backend API contract across the backend, frontend api.ts client, OpenAI commercial agent context, README, and deployment documentation. The final contract must include GET /health, GET /products, POST /products, PUT /products/:id, PUT /products/:id/pricing-rule, POST /products/:id/inventory-movements, GET /webhooks/whatsapp, POST /webhooks/whatsapp, GET /conversations with optional status query, GET /conversations/:id, GET /dashboard/review-queue, POST /dashboard/conversations/close-stale, POST /dashboard/conversations/:id/take, POST /dashboard/conversations/:id/reply, POST /dashboard/conversations/:id/manual-offer, POST /conversations/:id/suggest-reply, POST /negotiations/:id/accept, POST /orders/:id/payment-link, GET /payments/:externalId, POST /payments/:externalId/confirm, POST /webhooks/payments, and POST /orders/:id/delivery. Keep request and response payloads aligned with the TypeScript schemas and frontend types.
```

