# D3-P03 - Railway Backend Environment

Author: SCDF  
Delivery: Delivery 3 - Final Functional Product  
Prompt language: English  

## Prompt

```text
Create the Railway backend environment reference for the deployed ComercIA backend. Include NODE_ENV, PORT, DATABASE_URL, DATABASE_SSL=true, FRONTEND_URL, ALLOWED_ORIGINS, DEFAULT_STORE_NAME, DEFAULT_STORE_PHONE, PAYMENT_BASE_URL pointing to the frontend /pay route, CONVERSATION_CLOSE_AFTER_MINUTES, WHATSAPP_PROVIDER=meta, META_GRAPH_API_VERSION, META_WHATSAPP_VERIFY_TOKEN, META_WHATSAPP_ACCESS_TOKEN, META_WHATSAPP_PHONE_NUMBER_ID, META_APP_SECRET, OPENAI_API_KEY, OPENAI_MODEL, and optional unused AWS placeholders. Document that Railway JSON values must be strings and that secrets must not be committed.
```

