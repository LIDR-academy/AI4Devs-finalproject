# D3-P06 - Production Health and CORS

Author: SCDF  
Delivery: Delivery 3 - Final Functional Product  
Prompt language: English  

## Prompt

```text
Implement and verify production health and CORS behavior for ComercIA. The backend must expose GET /health returning ok=true, service=comercia-backend, and database=postgres. Configure CORS from ALLOWED_ORIGINS or FRONTEND_URL, allow the deployed frontend Railway origin, support local development, and return clear JSON errors without exposing secrets when configuration or database access fails.
```

