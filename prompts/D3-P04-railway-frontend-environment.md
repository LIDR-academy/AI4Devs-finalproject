# D3-P04 - Railway Frontend Environment

Author: SCDF  
Delivery: Delivery 3 - Final Functional Product  
Prompt language: English  

## Prompt

```text
Create the Railway frontend environment reference for the deployed ComercIA frontend. Include VITE_API_URL pointing to the backend Railway URL and explain that Vite reads this value at build time, so Railway must redeploy the frontend after changing it. The API client must trim trailing slashes, use localhost only in development, and keep the current deployed backend URL as the production fallback.
```

