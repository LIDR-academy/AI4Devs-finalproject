# Base del API Gateway. Vacío = rutas relativas /api… (mismo origen: proxy Vite en dev o mismo host en prod).
# Si el SPA y el gateway no comparten origen, define la URL absoluta (p. ej. http://localhost:8080) y CORS en gateway.
VITE_GATEWAY_BASE_URL=
VITE_OIDC_ISSUER=http://localhost:8180/realms/mtl
VITE_OIDC_CLIENT_ID=mtl-spa
VITE_OIDC_SCOPE=openid profile email
