package ai4devs.frm.domain;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

/**
 * Sample unit test to verify the test infrastructure is working.
 * Unit tests should be fast and not require Spring context.
 */
class SampleUnitTest {

    @Test
    void sampleTest() {
        // Simple unit test without Spring context
        int result = 2 + 2;
        assertEquals(4, result, "Basic math should work");
    }
}
