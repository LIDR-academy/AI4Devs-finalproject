import { execSync } from 'child_process';

// Configuración global para tests E2E
beforeAll(async () => {
  // Aquí se pueden ejecutar migraciones de base de datos de prueba
  // o configuraciones iniciales necesarias
});

afterAll(async () => {
  // Limpieza después de todos los tests
});
