# Documentation Index

Technical documentation for this Laravel 13 + Livewire 4 application, kept in sync with the real code by the [`docs-maintainer`](../.claude/skills/docs-maintainer/SKILL.md) skill. If something here contradicts the code, the code is right — flag it or fix the doc.

## Contracts

- [Contracts](contracts.md) — behavioral contracts governing what the AI agent may or may not do, and how it should make decisions, while working in this repository.

## Workflow

- [Workflow](workflow.md) — the required multi-agent orchestration process (Three Amigos + TDD + security audit + code review + continuous docs) the project's Claude Code agents follow to carry a task from definition to closure, phase by phase, including the three-stage task-storage convention (`ai-spec/tasks/` → `in-progress/` → `done/`) and the mandatory **two-direction** link-integrity check `docs-keeper` runs on every stage move: a task file's own *outbound* relative links silently break when it moves to a deeper directory, and — since task 0010's closure — every *inbound* link to it from a file that never moved breaks too, which is the half nobody opens while reviewing the move.
- [`.claude/skills/three-amigos-debate/SKILL.md`](../.claude/skills/three-amigos-debate/SKILL.md) — the skill that automates [Phase 1](workflow.md#phase-1--three-amigos-debate) of that workflow: `/three-amigos-debate epic <n>` decomposes a [PRD](PRD/PRD.md) epic into candidate stories (pausing for your confirmation), `/three-amigos-debate story <description>` debates a single ad-hoc story, and either way it convenes the expert/QA/database agents and writes one User Story file per story to `./ai-spec/tasks/`. It stops at Phase 1 — no code, no INVEST check, no TDD.

## Product Requirements

- [PRD](PRD/PRD.md) — product-level requirements for the Arospe backoffice (Users/Roles, Products/Taxes/Shipping, Blog, Internationalization): per-epic Gherkin scenarios, acceptance criteria, roadmap, and scope boundaries; feeds the Three Amigos process for individual tasks.

## Architecture

- [Overview](architecture/overview.md) — what this app is today, the real request lifecycle (routes → Livewire/controllers → Gate & policies → actions → models → DB), runtime dependencies (MySQL, database-backed session/cache/queue), and the "Where things live" layer table — which since task 0013 also names the **declarative UI registry** layer (`config/modules.php` + the one component reading it), and since task 0015a the `app/Actions/Auth/**` cross-cutting-concern folder and `app/Exceptions/**` (three response-rendering domain exceptions the table had never listed). Task 0015a is also the first story since 0004 to change the **request-lifecycle diagram** itself: a `Step-up guard` node reached by both the Livewire component and the domain actions, after the Gate and reading the session. Task 0015b deliberately leaves that diagram alone — refusal logging is a side effect of the `Gate` node already there, not a new step — and only corrects the `app/Actions/Auth/**` layer row, which named one class in a folder that now holds two. Task 0017 likewise adds no lifecycle step — a third module screen walks the path already drawn — but it is the pass that found this page's **oldest** stale sentence: the second paragraph still opened *"The domain layer beyond `App\Models\User` does not exist yet"*, falsified by task 0016, whose own docs pass corrected the identical claim in [database/schema.md](database/schema.md#notes) and missed this copy. Four diagram node labels were also enumerations that had gone out of date (`Routes` named three of four route files, `Gate + policies` one of three policies, `Domain actions` one of three folders, `Eloquent models` one of three models) and are now the layer with examples rather than a list to maintain.
- [Authentication](architecture/authentication.md) — Fortify-based registration, login, password reset, two-factor authentication, and passkeys, plus the account lifecycle layered on top: the `users.status` / `App\Enums\UserStatus` account state, the no-self-activation invariant and the single `ActivateVerifiedUser` listener all three verification paths converge on, the two non-HTTP callers of the reset flow (the Super Admin bootstrap, and `CreateUser`'s administrator invitation), and the pending-email mechanism (an email change is parked in `users.pending_email` and only applied once its signed, address-bound, 60-minute link is used) shared by the profile screen and the admin user editor alike, why a soft-deleted account stops authenticating on every path without any check in `app/` refusing it, and — since task 0007 — the **sign-in block** that makes `users.status` an authentication control rather than a label: the three enforcement points a fresh session can be refused at (`Fortify::authenticateUsing()` for password and two-factor, `Passkeys::authorizeLoginUsing()` for passkeys, and the `Login`/`Authenticated` listener pair for remember-me recall and the suspended-mid-challenge race), what the refusal deliberately does and does not disclose, why an already-live session is deliberately left alone, and the third guard `ActivateVerifiedUser` needed once `Inactive` started denying access. Since task 0015 two of its claims are corrected rather than extended: `UserInvitation` is deliberately **not** `ShouldQueue` (its constructor holds a `Password::broker()` token, which a queued send wrote in plaintext into a `jobs` row — the rule being that a notification whose constructor holds a credential must not be queued, while `PendingEmailVerification` stays correctly queued because its signed URL is built in `toMail()`), and the pending-email throttle is **two** limiters rather than one target-keyed limiter that "every present and future caller shares". Since task 0015a it also records that Fortify's **password-confirmation flow has a second consumer outside the auth screens** — the Users screen's step-up guard, reusing the same session key and the same `auth.password_timeout` with no second flow and no second timeout — and the two facts that belong to this page rather than to the authorization one: `POST /user/confirm-password` is now rate-limited **by this app** (5/min) because Fortify's routes consult no limiter config key for it, and the confirmation's session scoping (including why `SessionGuard::login()`'s `migrate(true)` would let a session inherit the previous user's confirmation if either logout path skipped `Session::invalidate()`) is what makes the key usable as a step-up control at all. The single source of truth for how auth works.
- [Authorization](architecture/authorization.md) — `spatie/laravel-permission` roles & permissions, now a working foundation *in real use*: the two seeded roles (`Super Admin`, `Administrator`), the 42-permission `<module-slug>.<action>` catalog and who holds what, the seeder (including the five-branch Super Admin bootstrap driven by `SUPER_ADMIN_EMAIL`), the `Gate::before` Super Admin bypass with its coverage gap and the "gate on permissions, never role names" convention, the three middleware aliases, and — since task 0004 — the first permission-gated route (`users.index`) plus the **Policies** section covering `UserPolicy`'s seven abilities — including `delete()`'s trashed-target refusal from task 0005 and why a policy-level rule is one the Super Admin bypass still reaches — why a Livewire route must gate with `can:` rather than `permission:`, and why the Users list's per-row `Gate::allows()` calls (which disable a row action the actor may not perform) are a **UI hint** that must reuse the same policy method and never substitutes for the mutating methods' own `Gate::authorize`. Since task 0008 it also owns **The Super Admin role's invariants** — the `App\Models\Role` subclass that makes the seeded role categorically undeletable, unmodifiable in every direction, unacquirable by any other role and invisible to roles-list queries: the one-role-model convention and the two `arch()` rules enforcing it, `Role::superAdminName()` as the single resolution path the `Gate::before` bypass now shares with the guards, the three guard layers (with the `boot()`-not-`booted()` vendor-ordering trap and the two identity helpers that read different sources on purpose), the `selectable()` local scope and why a global one was rejected, `RolePolicy` plus the bypass deferral that makes it independently effective against a Super Admin actor, the enumerated bypasses that remain open, and the partial-hydration residual the roles-CRUD screens inherited — **closed by task 0009**, which routed both `RolePolicy`'s Super Admin branch and the `Gate::before` deferral through `Role::isSuperAdminRoleRow()`. Since task 0008a it also owns **The Administrator tier's identity** — the second privilege tier, resolved through a deliberately *different* mechanism to the Super Admin one and for a stated reason (its name is locked, so `RoleName::Administrator` **is** the source of truth and no config key may be added): the one predicate in its two shapes (`Role::isAdministratorRole()` / `isSuperAdminRoleRow()` for a row, the enum value / `superAdminName()` for a name), the shared `persistedName()` that makes both hydration-safe, the relocation of the tier's authorization out of the Livewire component and **into** `CreateUser` / `UpdateUser` so a non-dashboard caller inherits it, why a rule that must bind a **Super Admin actor** is a direct `throw` rather than a `Gate` check, the seeder's collation read-back guard, and the two accepted asymmetries this leaves — a row action that renders enabled and is refused on click for one actor/target pair, and an update path that hard-refuses a Super Admin-holding target while the delete path still does not. Since task 0009 the **`RolePolicy`** section covers the *role* side of that tier (its Administrator-level branch on `update()`/`delete()`, why the branch order is load-bearing, and the two permission-name constants), and a new **Who may grant a permission — the meta-rule layer** section covers `App\Actions\Roles\EnforceAdministratorPermissionGrant`: the Super-Admin-only `grantAdministratorPermission` ability story 0011's UI consumes, why an omitted-but-already-granted permission is *preserved* rather than revoked when a full-set `syncPermissions()` meets a form the actor only partly sees (a human product decision), why the action reads the "before" state from the model instead of accepting it, and the transformer-not-writer limitation deferred to story 0010 — **now resolved** (decision G2: keep the split, with two `saveRole()` implementation rules in place of a contract change). Since task 0010 it also owns **The Administrator tier's immutability** — the tier's row is now name-locked and undeletable while its permission set stays fully editable, the deliberate asymmetry with the Super Admin tier laid out operation by operation, the three implementation details (`isDirty('name')` scoping, the two guards reading opposite sources, `delete()` diverging from `update()`), and the ⚠️ that `RolePolicy::delete()`'s Administrator branch is unreachable for a Super Admin actor because `Gate::before` only defers on a *Super Admin* target — plus **The second grant meta-rule: you cannot grant what you do not hold** (`App\Actions\Roles\EnforceGrantorPermissionScope`: why the `roles.manage-administrators` exclusion rather than call order keeps the two transformers consistent, why a Super Admin is exempt outright, why a grant-scope rule leaves revocation unrestricted by construction, and the ⚠️ that the two actions' opposite handling of an omission is safe only while the rendered catalog stays unfiltered — narrowed by task 0011 to name the **one** value the shipped view does withhold and what would have to change before a second joins it). `RolePolicy` now has **five** abilities and its first real call site, `App\Livewire\Roles\Index`. Since task 0011 the page's `Gate::allows()`-is-a-UI-hint section also carries its **second** call site — `Roles\Index::roles()`, the pseudo-attribute-not-array shape it uses and why — and the `RolePolicy::delete()` ⚠️ that was written forward-looking is now a shipped, dataset-pinned three-tier table on the `Administrator` row. Since task 0012 its **How to gate something** section owns the project's copyable **module-gate pattern** for PRD Epics 2–4: one `can:<permission>` per route as the plain alias string inside the area file's existing `['auth', 'verified']` group, the four vendor-verified properties that come with it (`can` needs no alias registration, the Super Admin bypass needs no special case, there is no `permission:a|b`-style OR form under `can:`, and a **misspelled ability denies silently** — so every gate needs a positive 200 test beside its 403), the three alternatives rejected and why (Spatie's `permission:`, a group-level gate, Laravel's `->can()` route sugar), and the ⚠️ that a `.view`-shaped gate leaves a role holding `create`/`edit`/`delete` on a module it cannot reach, fail-closed and unwarned — now narrowed by task 0013, which makes that misconfiguration silent in the navigation as well (correct, but *less* discoverable: the module simply is not there). Task 0013 also adds the pattern's other half, **The second half of a module gate: the sidebar registry** — this repo's first declarative permission-driven UI registry (`config/modules.php` + one anonymous `sidebar-nav` component + `lang/{en,es}/navigation.php`), which every later PRD epic extends by appending **one entry** rather than editing a component: the copyable entry shape and the five rules that come with it (an entry's `permissions` must be *exactly* its route's own `can:` ability, test-pinned so the two halves cannot drift; visibility resolves through `Gate::any()` and never `hasAnyPermission()`, a correctness fork because the zero-permission-row Super Admin would otherwise see an empty menu; `permissions: []` must be branched on explicitly since `Gate::any([])` is `false`; filter-before-group is what makes an emptied group's heading vanish *structurally* rather than by a remembered conditional; and the two `data-test` hook conventions that keep absence assertions off colliding words like "Settings"), plus a ⚠️ for the two hazards a later epic meets first, both unexercised today — `Gate::any()`'s **OR** semantics are invisible while every entry holds one ability, and a typo'd `group` key drops its item silently (fail-closed, but indistinguishable from "my module never shipped"). Task 0015 adds **no** ability, policy method or route and instead corrects where two existing abilities are asked: the `Gate::allows()`-is-a-UI-hint section's `canEdit` quote and its "same policy method" bullet were falsified (the hint now mirrors `openEditModal()`'s `updateSensitiveAttributes` gate plus a self-row identity check, not `save()`'s `update`), its single accepted drift became **two** — the second being `canDelete` rendering the actor's **own** row enabled while `deleteUser()`'s self-delete guard no-ops the click — and both statements that the delete path has *no* direct-throw-style guard are narrowed: the self-targeting half is closed, the other-holder half is not. `UserPolicy`'s "who calls what" paragraph no longer claims `updateSensitiveAttributes` is asked **only** in the actions. Since task 0015a the page owns a **third authorization layer** in [**Step-up authentication**](architecture/authorization.md#step-up-authentication--the-third-layer) — the first structural addition to this page rather than a rule inside an existing section: the table naming the question each layer answers (route middleware asks about the *account*, a policy about the *actor/target pair*, step-up about **the person at the keyboard**, which a hijacked or unattended session passes trivially at the first two), the five operations it covers and the four it deliberately does not, why a self-edit and an ordinary-role creation are exempt **structurally** rather than by a second condition, why it is an in-method check (`RequirePassword` is absent from Livewire's `PersistentMiddleware` allow-list — the same fork that produced `can:` over `permission:`), the three independent reasons it is not a `UserPolicy` ability (`Gate::before` would exempt the Super Admin it most needs to bind; freshness is a property of the *session*, not of the pair; and a 403 is indistinguishable from "you lack the permission", so it renders **423**), the ordering rule with a flowchart (the permission refusal always wins, and a branch with no preceding `Gate` call is not an exemption), the two caller shapes (423 for a direct caller, a logged redirect for the dashboard), and the UI-hint rule — plus two ⚠️s: the endpoint every later step-up-gated screen will inherit as its barrier now that this story had to add the rate limit Fortify does not ship, and the two doors the layer still does not close. Since task 0015b the page owns its **third copyable pattern** for a later epic, beside the module gate and the sidebar registry — [**Recording a refusal**](architecture/authorization.md#recording-a-refusal--what-every-gate-owes-the-audit-trail), the observability counterpart to all three layers above it: the one `Log::warning('Privileged action refused', …)` line and why each of its four properties is a decision (`warning` over `info`; deliberately **generic** `target_type`/`target_id` keys rather than a fourth per-domain shape; a constant, never-interpolated message; and no payload, asserted against the context array rather than a rendered string), the shared helper's two halves built like `EnsureRecentPasswordConfirmation`'s so the record half and the refuse half cannot drift, the three reasons the `denies()`-then-`authorize()` wrapper beats a per-site `try/catch` (one implementation of the rule; a `catch` around `Gate::authorize()` misattributes a *nested* refusal to this ability; the actor is a **parameter**, so a queued-job caller does not log `actor_id: null`), the snake_case reason strings the non-`Gate` sites carry in place of an ability name, the log-ceiling rule for a rate-limit site shared with an unprivileged caller (gate the **log**, never the limit, on a distinct key prefix keyed as narrowly as the refusal), the three deliberate exclusions (both `mount()`s, the step-up line, `App\Models\Role`'s model-event guards) and the five-step checklist a third admin screen follows — plus a ⚠️ that a refusal on these screens produces one of **two** message strings, since story 0015a's `'Step-up password confirmation required'` is deliberately left unfolded: a level filter catches both, a message filter needs both. Since task 0017 the page carries the first evidence that any of its three copyable patterns **generalises**: `sales-regions.index` is the third permission-gated route and [`SalesRegionPolicy`](architecture/authorization.md#salesregionpolicy--the-third-policy-and-the-first-with-no-target-branch) the third policy, both built by *following* the module gate and the refusal-logging recipe rather than by producing them — the recipe's ✅ records that it held without a sixth step, that the generic `target_type`/`target_id` keys absorbed a new domain with no change to the line (though `resolveTarget()` auto-resolves only `User` and `Role`, so a new domain passes them explicitly), and that **step 3, the log ceiling, is the one step 0017 had no occasion to exercise**. `SalesRegionPolicy` is documented as the smallest and most instructive policy to read *against* the other two, since almost everything it does not do is a recorded decision: two abilities rather than four (`create`/`delete` are seeded and deliberately unused — the catalog is fixed), one permission tier gating even the default swap (the catalog holds no candidate second string, so inventing one is a seeder change), and a `update()` that ignores its `$target` entirely — which makes this the first screen whose per-row `Gate::allows()` hint has **no accepted drift**, a property of the empty policy body rather than a guarantee, with the ⚠️ that a future target-dependent branch must be evaluated against a **re-fetched** row. The story's genuinely new concept gets its own boundary section, [A domain invariant is not an authorization rule](architecture/authorization.md#a-domain-invariant-is-not-an-authorization-rule-and-does-not-live-here): it answers *"would the data still be valid"* rather than *"may this actor"*, so it can never be a policy method (a Super Admin can leave the catalog defaultless exactly like anyone else — the Super-Admin-actor rule reached from the data side), it runs strictly after authorization, its subject must be re-read under lock while an authorization check's need not be, and it is still logged like a refusal. Its mechanics stay on [security/model-instance-trust.md](security/model-instance-trust.md) and are pointed at rather than duplicated. Task 0018 closes the one gap that entry recorded — 0017 shipped **no** `config/modules.php` entry, so the screen was reachable only by URI — and in doing so completes the set: 0017 exercised the module gate and the refusal-logging recipe, 0018 exercises the third pattern, [the sidebar registry](architecture/authorization.md#the-second-half-of-a-module-gate-the-sidebar-registry), whose ✅ block now records that the cost really was **two array literals and two lang leaves with the reading component untouched** — the "append data, never behavior" claim, verified against the diff rather than asserted — plus the three things the first three entries could not show, all of them single lowercase words in pre-existing groups: a multi-word item key is **`snake_case`** and is simultaneously the config key, the translation leaf and the `data-test` hook; **adding a group is data too** (`taxes` being the first non-`expandable` group carrying an icon); and **both generic drift guards picked the entry up for free**, so the registry↔route cross-check needed no edit — stated because the story's own plan assumed the opposite and would have hand-written a redundant copy of a check that already generalises. `SalesRegionPolicy`'s **no accepted drift** claim also acquired its first rendered consumer and survived it, with the distinction that screen makes visible: a row control can render `disabled` while `canEdit` is `true`, for a **domain-invariant** reason rather than an authorization one, so the disabled set is a superset of the hint by construction and must not be read as drift. Story **0019** changes the two things this entry states as fixed numbers: the catalog is the **42**-permission `<module-slug>.<action>` grid (ten modules × four, plus the two role-management names), `Administrator` holds **41 of 42**, and `media` is the first slug appended since task 0002 — with [The `media` module, and what a catalog amendment costs](architecture/authorization.md#the-media-module-and-what-a-catalog-amendment-costs) recording that the production diff really is one line while the *cost* is fifteen hardcoded test assertions plus prose in eight documents, and a ⚠️ claiming the amendment shipped **without** its `roles.modules.media` translation leaf — **false, and corrected by story 0020**: both leaves are present and always were. The rule that ⚠️ hangs on is kept, because a missing leaf really would render a raw key with a fully green suite; `media` is now its evidence that the rule is followable rather than its evidence that it gets missed. See [errors-log.md](errors-log.md#one-docs-pass-reported-two-gaps-that-were-not-there-both-marked-verified--2026-08-29). It also brings the **fourth** policy, [`MediaPolicy`](architecture/authorization.md#mediapolicy--the-fourth-policy-and-the-first-behind-no-route-at-all) — so every count on that page naming three policies is now four — the second with no target-dependent branch, and the first defending a model with **no route in front of it**: PRD §2.3 makes the gallery a modal Products and Blog embed, so there is no `can:` middleware and the in-component `Gate::authorize()` calls are the entire perimeter rather than the second of two layers. Its four-abilities-two-used shape is recorded as a *different* decision from `SalesRegionPolicy`'s two-of-four (define an ability when you can name what will ask it). **That entry also carried this doc set's sharpest recent mistake, corrected by story 0020**: it claimed *"this screen logs no refusals at all — `LogRefusedPrivilegedAttempt` appears nowhere under `app/{Livewire,Actions}/Media/`, verified by grep"*, which was **false on the day it was written** — the docs pass that found the gap is what got it closed, the fix was rebased into the earlier feature commit, and the ⚠️ shipped anyway and stood for a whole story. It is now a ✅ recording the shipped state, and [errors-log.md](errors-log.md#one-docs-pass-reported-two-gaps-that-were-not-there-both-marked-verified--2026-08-29) owns the lesson. Story **0020** then adds the page's **fourth copyable pattern**, and the first to arrive as a real Phase 4 Medium rather than as a retrospective: [a routeless Livewire component has no per-request authorization backstop](architecture/authorization.md#a-routeless-livewire-component-has-no-per-request-authorization-backstop) — the exact negative of the module-gate pattern at the other end of that page. `mount()`-only gating is correct for the three **routed** screens, because `can:` is on Livewire's `PersistentMiddleware` allow-list and is replayed on every `/livewire/update` round trip; a component with **no route** has no middleware of its own to replay (Livewire replays the *host page's*, which says nothing about `media.*`), so `mount()` runs once and every later call arrives unauthorized — finding **F-1**, under which `tiles()`/`toggleSelect()`/`confirmSelection()` kept disclosing and accepting selections across the whole library after `media.view` was revoked mid-session. Its real content is the asymmetry: **a render-path method fails closed, an action method throws**, because a throwing `#[Computed]` propagates out of the child and 500s the *host* page — the failure the consumer-side `@can` embed exists to prevent, arriving one layer lower — plus three things that generalise (enumerate **methods**, not abilities; a `#[Computed]` property **is** a method; a disclosure gate may legitimately ask a **stronger** ability than its neighbours) and a ⚠️ that the three routed screens are safe under the *current* reading of their own routes rather than permanently. `MediaPolicy`'s ability table is now four-of-four used, `update()` having gained three call sites including `App\Actions\Media\UpdateMediaDetails`. Story **0021** adds this page's [routeless-component pattern](architecture/authorization.md#a-routeless-livewire-component-has-no-per-request-authorization-backstop) its **second** real instance — `App\Livewire\Components\WysiwygEditor`, itself routeless and itself a `Gallery` consumer, shipped its first cut with `openGallery()`/`insertImage()` ungated (the section's own ❌ shape, rediscovered rather than avoided) and was fixed the identical way (Phase 4 finding F-2). No new permission, role, policy or catalog change — the story reuses `MediaPolicy`'s existing `viewAny` ability rather than defining one of its own.

## Database

- [Schema](database/schema.md) — ER diagram and column-level description of every table (`users` including `status`, `pending_email` and `deleted_at`, `passkeys`, `sessions`, the permission tables) and how they relate, plus a **Soft deletes** section on what deleting a user rewrites (email obfuscation, `pending_email` clearing, `password_reset_tokens` revocation), what physically survives it, and the two deliberate index omissions — and, on the `roles` table, what the `utf8mb4_unicode_ci` collation on `roles.name` means for a byte-exact identity check (both seeded-role creation paths read the persisted name back and throw on a case-insensitive collision). Since task 0016 it also owns [**`sales_regions`**](database/schema.md#sales_regions), the first Epic 2 domain table and the first one created greenfield with a UUID PK: its columns (including why `rate` is a nullable `DECIMAL(6,3)` and never a `float`, and why `NULL` and `0.000` cannot share a meaning), the self-referencing `parent_id` tree that is exactly one level deep and the `kind` ⟺ `parent_id IS NOT NULL` invariant the seeder — not the database — enforces, the seeded state (~254 rows, 6 active, one default), the **seeder-owned vs. administrator-configurable column split** that makes `upsert()` the wrong shape here, the verified three-index list with its four deliberate omissions, and the ⚠️ that nothing in the database enforces at-most-one `is_default`. Since story 0019 it also owns [**`media`**](database/schema.md#media), the Shared Media Gallery's table and the first in this app whose rows point at **files on disk**: the columns in verified physical order, why `NOT NULL` on `webp_path`/`avif_path` *is* the schema-level statement of PRD §2.3 AC 4, the three-explicit-path-columns decision with both alternatives rejected (a `conversions` JSON blob, a `media_conversions` child table) and the rule not to derive variant paths at read time, the two deliberately omitted columns (`disk`, `mime_type`), the absence of `deleted_at` because the referential question a delete raises cannot be settled before the Products and Blog tables exist, the three real indexes and four omissions (including why search is a `LIKE` scan and `FULLTEXT` would *break* partial matching rather than merely cost a write), and two ⚠️s worth finding before someone "fixes" them — `nullOnDelete()` on `uploaded_by` will essentially **never fire**, because `users` is soft-deleted and a delete there is an `UPDATE`, and this table's contents are only half in the database, which is why `composer.json`'s `setup` script gained `@php artisan storage:link` in the same story.
- [Migrations](database/migrations.md) — this repo's real migration conventions (naming, `down()` symmetry, adding columns, backfilling when a new column's default is wrong for existing rows, dropping a unique index before its column), with examples pulled from `database/migrations/`. Its **UUID primary keys** section is no longer an aspirational snippet: since task 0016 it quotes the repo's first real greenfield UUID `create_*` migration (`create_sales_regions_table`) and the four decisions it establishes — `foreignUuid` over `foreignId`, an explicit table name on a self-referencing `constrained()`, `restrictOnDelete()` where a cascade would destroy administrator-configured data, and length-capped strings — plus the rule that **an FK column does not also get an explicit `index()`** here, which deliberately diverges from `create_passkeys_table`'s `$table->index('user_id')`. Story 0019's `create_media_table` is that pattern's **second** instance and needed no exception — recorded as a ✅ rather than assumed, since a convention with one instance is a description of that instance — and it gives the *explicit table name* rule its clearest case: `constrained('users')` on a column named `uploaded_by`, where Laravel would infer an `uploadeds` table, a wrong guess far less obvious in review than `sales_regions`'s `parents`. It also carries a ⚠️ that `nullOnDelete()` against a **soft-deleted** parent is documentation rather than behaviour — `users` deletes are `UPDATE`s, so the clause will essentially never fire.

## API

- [Routes](api/routes.md) — the real contract surface today: app-owned web/Livewire routes across `web.php` and its three per-area files, `roles.index` (tasks 0010 + 0011 — the **second** permission-gated route, now rendering end to end: what `resources/views/livewire/roles.blade.php` puts on screen, the view-side `groupBy()` that derives the per-module permission grouping and its composed labels, the fail-closed delete modal and the three inline error outlets behind it, the component's public surface including the per-row `canEdit`/`canDelete` UI hint and its one accepted Super Admin/`Administrator`-row drift, the icon-only `data-test` hooks present on both branches, the "catalog renders in full, with exactly one deliberate omission" rule, and the two refusals that are deliberately *not* 403s), `users.index` (the first permission-gated route, why its middleware column understates what protects it, what its now-shipped view renders, its icon-only `data-test` row-action selectors — present on both the enabled and the policy-disabled branch of each row action — the two Flux/Blaze markup rules that disabled branch established, the one accepted case in which that disabled state drifts from what a click actually does, and — since task 0013 — the **gated** sidebar link to it), the `email-change.confirm` controller route (the first one deliberately registered outside the `auth` groups), plus the Fortify- and Passkeys-vendored auth routes. Since task 0012 its App-owned-routes preamble also points a later epic's developer at where the *reusable* gate pattern lives (`architecture/authorization.md`, not here) and names `tests/Feature/Authorization/ModuleRouteAccessTest.php` as where both routes' cross-gate independence, Super Admin bypass, non-disclosure and next-request cache behaviour are pinned — that story changed no route's URI, name or middleware. Since task 0013 its App-owned-routes preamble also records that a gated module route is only **half** the job — shipping a module screen means adding its route *and* one `config/modules.php` registry entry naming the same single ability, or the screen exists with nothing linking to it — and both gated subsections' sidebar bullets, which had claimed since tasks 0006/0011 that each route was "linked from the sidebar with no permission gating" and that permission-aware navigation "is not yet built", are rewritten around the shipped gating. Since task 0015 the `users.index` subsection corrects two now-false claims about that route's *behaviour* (its contract is unchanged): the per-row `canEdit` no longer comes from `Gate::allows('update', …)`, and an Administrator-holding **other** target no longer renders editable to an actor holding only `users.edit` — plus a seventh bullet for the self-delete no-op and why `canDelete` still renders the actor's own row enabled, and a note that the three modal openers, ungated since task 0004, now authorize. Since task 0015a that bullet list runs to **nine**: holding the permission is no longer sufficient for five of the screen's operations (another user's role, status or email; a deletion; an Administrator-tier creation), each additionally requiring a password confirmation no older than `config('auth.password_timeout')` — enforced in-method and therefore invisible in the middleware column, with two response shapes a caller must not conflate (**423** for a direct caller, a logged redirect to `password.confirm` for the dashboard) and the four deliberately-uncovered cases named; plus the three modal `data-test` notice hooks and the one DOM-contract knock-on, the role select's `wire:model.live`. The `security.edit` "only route with the extra `password.confirm` requirement" sentence is narrowed to *middleware*, and **Fortify-owned auth routes** gains the app-added `throttle:confirm-password` on `password.confirm.store` — visible in `route:list` but unexplained by `config/fortify.php`, which carries no limiter key for that route. Since task 0017 the table carries its **first new app-owned route since task 0010** — `sales-regions.index` (`GET /taxes/sales-regions`, `can:sales-regions.view`), the third permission-gated route and the first of PRD Epic 2's Products/Taxes domain — with a subsection scoped honestly to what shipped: the **view is a deliberate placeholder** (story 0018 owns the real markup), the route is the half of a module gate that currently has **no** `config/modules.php` entry, and `ModuleRouteAccessTest.php` was not extended, so its cross-gate assertions still cover two routes while three exist. Since task 0018 the first two of those three statements are **false and corrected in place**: the view is the full screen and the registry entry exists, so that subsection's bullet list runs from nine (0017's backend contract) to twenty (0018's screen) — what the markup renders, the **F-1 carve-out** that keeps a parent-with-children row in the active table regardless of its own `isActive` (without it, deactivating "España" made its whole fiscal-territory group, default badge and configured rates vanish from both tables), the eight `data-test` hook families and why a page-global `assertSee('0%')` is unsafe, the two *different* disabled-toggle tooltips, the two rendering sites for the `replacementDefaultId` refusal and the `@unless ($showModal)` guard between them, `closeModal()` gaining `resetValidation()` (the story's one cross-story edit into 0017's component, and a blocking bug before it landed), the `<input type="checkbox">` + `wire:click="$toggle('active')"` binding and why neither half is the obvious one, `type="text" inputmode="decimal"`, the string-only rate rendering, the three `Js::from()` `wire:click` attributes that must not be converted to `@js()`, and the registry entry down to its snake_case key. `ModuleRouteAccessTest.php` is **still** not extended — that gap is 0017's and 0018 did not close it. Story 0019 adds **no route at all** and one ⚠️ to the App-owned-routes preamble, which is the only reason that page needed a pass: it introduces the app's first **permission-gated surface with no route in front of it** (`App\Livewire\Media\Gallery`, a modal Products and Blog embed per PRD §2.3, reached over `/livewire/update`), so this file's own recurring caveat — *"the middleware column understates what protects it"* — reaches its limit, since for that component there is no middleware column at all. Recorded with the instruction **not** to add a `GET /media` route or sidebar entry to make it symmetrical: the absence is a decision, unlike the incomplete half `sales-regions.index` sat in between tasks 0017 and 0018. Story **0020** adds no production route either, and promotes that ⚠️ into two full subsections, because a picker story binding to the gallery needs the contract and not only the warning. [`App\Livewire\Media\Gallery`](api/routes.md#applivewiremediagallery--a-gated-surface-with-no-route-and-how-a-consumer-embeds-it) now carries nine bullets in the shape the three gated-route subsections use: the four-attribute `@can`-wrapped embed that *is* the whole integration surface, why `@can` is a layer and never the gate, the **six** self-authorizing methods and why `tiles()` fails **closed** where its two neighbours throw, why the return event name is consumer-supplied rather than `->to()`-targeted (`dispatchTo()` resolves by component *name*, so it broadcasts to every mounted instance — and story 0027 embeds two), the one-shape-both-modes payload re-fetched from the database at confirm time, the two caps (60 tiles, 3 files — the latter *measured* down from 5 against PHP's 30 s `max_execution_time`) and why neither can drop a staged selection, the fifteen `data-test` hooks, and the ⚠️ that a role without `media.view` loses the gallery with no error at all. It also corrects the 0019 sentence saying the gates live "inside `mount()` and `upload()`" — two of six now. The second subsection is [`dev.media-gallery-harness`](api/routes.md#devmedia-gallery-harness--temporary-environment-gated-scaffolding), the **one route this file deliberately keeps out of the table**: temporary browser-test scaffolding registered only under `testing`/`local`, gated at *registration* time (non-existence, not refusal) with a `mount()`-level `abort_unless(…, 404)` behind it for the stale-`route:cache` case, pinned by a test asserting absence from the route **collection**, and carrying story **0027** as its named deletion trigger — if 0027 has shipped and that subsection still exists, the scaffolding was not removed. There is no REST API yet; this file explains what replaces it once one exists. Story **0021** adds no route either, and repeats the shape one layer out: [`App\Livewire\Components\WysiwygEditor`](api/routes.md#applivewirecomponentswysiwygeditor--the-gallerys-first-real-consumer-and-the-second-routeless-gated-component) is `Gallery`'s **first real consumer**, itself routeless, with its own subsection — the hard sanitize-on-write dependency behind why `$value` is deliberately not `#[Locked]` (verified closed today across all four named consumer stories), the per-instance-derived `select-event` that **corrects the rationale 0020's own D2 states** (it is listener-name uniqueness, not "DOM bubbling plus `#[On(...)]`" — Livewire registers every listener page-globally), the Phase 4 fix gating `openGallery()`/`insertImage()`, and the app's **first `wire:ignore` region**. [`dev.media-gallery-harness`](api/routes.md#devmedia-gallery-harness--temporary-environment-gated-scaffolding) is extended rather than replaced — two `WysiwygEditor` instances join its two pre-existing `Gallery` instances, and its "deleted by 0027" comment now names both 0020 and 0021.

## Conventions

- [Base standards](conventions/base-standards.md) — stack versions, directory layout (including `app/Actions/Users/`, `app/Enums/`, `app/Listeners/`, `app/Notifications/`, `app/Policies/`, `lang/`), model conventions (attribute-based `#[Fillable]`/`#[Hidden]`, `casts()`, omission-from-`#[Fillable]` as the mass-assignment guard, and why a delete must go through the model instance rather than the query builder), the class-based Livewire component convention, when a controller belongs in front of an action, **why an authorization rule belongs to the action rather than to one of its callers** (task 0008a — move the rule instead of copying it, derive a security-relevant flag internally instead of accepting it as a parameter, authorize before the first write against freshly-reloaded state), **when an app-owned config file is the right shape and what it must never contain** (task 0013 — `config/modules.php` is the first config file here that is neither Laravel's nor a package's: a declarative registry a later story extends by appending *data*, never behavior, with no closures anywhere because `config:cache` serialises with `var_export()` and throws on anything else, and translation *keys* rather than copy), and the artisan-first/test-first quality gates — including, since task 0010, why steps 1 and 2 (`test --filter`, `pint --dirty`) are the *iteration* forms and must each be re-run **unscoped** before work is declared done. Since task 0015 the action-owns-the-rule convention also states its **converse**, so nobody "fixes" a Livewire disclosure gate by moving it into an action that does not perform the disclosure: the rule follows the *operation*, and what may never reappear in a component is a re-derivation of the target's tier. Since task 0015a that convention carries a second blockquote for a **weaker** case — the step-up deletion guard sits in `App\Livewire\Users\Index::deleteUser()` only because no `DeleteUser` action exists to hold it, an accepted placement pending a class rather than a second exception, recorded in the method's own docblock so a reader can tell "belongs here" from "is here for now" — and the directory listing gains `app/Actions/Auth/`, the first subfolder that is **not** a module area and deliberately not `Fortify/`, with the rule it establishes: a class serving two areas belongs to the named cross-cutting concern, never to whichever area called it first and never to a `Shared/`/`Common/` catch-all. Task 0015b gives that folder its second inhabitant and the case that **confirms** the rule rather than merely following it — `LogRefusedPrivilegedAttempt` is imported by seven classes across both module areas, so "wherever the first caller lives" would have left the Roles screen importing a Users class to record a Roles refusal. Since task 0017 the action-owns-the-rule convention has its first story where it **cost nothing**, applied at Phase 1 rather than found at Phase 4 — with the two things that generalise (**authorize every row the operation writes**, the row-level counterpart of task 0004's attribute-level rule; and **a component that authorizes as well is a layer, not a redundancy**) and the ⚠️ that this page's own "authorize before the first write" and "re-read what you authorize against" rules pull in **opposite** directions once an action locks its own rows. The `routes/` one-per-area paragraph now names the copyable case: a new area file is an existing one with three strings changed, `web.php`'s whole diff is one `require` line, and the `Index` import is aliased because the class name alone no longer says which of three areas it belongs to. Since task 0018 the **app-owned-config rule has its ✅**: `config/modules.php` had never been extended by anything since task 0013 wrote it, so "a registry a later story extends by appending data, never behavior" was a claim about a file with one author — 0018 is that later story, it cost two appended array literals plus one lang leaf per locale with no PHP class and no component change, and both hard constraints held on first contact (`expanded_when` went in as a literal `null` rather than the closure over `request()` the rule warns about). The `tests/Browser/` line of the directory listing was also corrected — it described the suite as mirroring the app structure while two of its three files sit flat. Since story 0019 the directory listing carries `app/Actions/Media/` (the fourth area folder, whose two-class split is the copyable part — `GenerateImageConversions` is the **only** class in the application that imports the imaging library), `app/Livewire/Media/`, `Media` and `MediaPolicy`, and two more published vendor config files, one of which (`config/livewire.php`) was published for a **security** reason rather than a preference: Livewire's own temporary-upload endpoint shipped a 12 MB, type-unrestricted default reachable by any `media.view` holder *before* this app's rules ever ran. The `#[Fillable]`-omission guard also gains its most lopsided case — `Media`, three fillable columns against **seven** omitted — which is where the test this convention really applies becomes clear: not "is this sensitive" but *could a form legitimately supply this value at all*, since a client-asserted width is not a width and a client-supplied path is an arbitrary write into a web-served directory; with the ⚠️ that the omission is a **mass-assignment** guard and not an integrity one — narrowed by story 0020, which is where a `media` row first gets updated at all and turns out to be the case that shows the guard **working** rather than failing: `App\Actions\Media\UpdateMediaDetails` writes through `$media->update([...])` with a literal two-key array, so the seven omitted columns stay unreachable and the write is auditable by reading one line; the residual (a future caller assigning a path column and calling `save()`, which no test would catch) is restated rather than closed. Story 0020 also adds [`app/Livewire/Dev/`](conventions/base-standards.md#directory-structure), the first subfolder here that is neither a functional area nor **permanent** — a test-only host page for the routeless media gallery — with the four rules that separate scaffolding from surface: the route is gated at *registration* time rather than by middleware (non-existence, not refusal), it carries `auth`+`verified` plus a `mount()`-level `abort_unless(…, 404)` for the stale-`route:cache` case the registration gate structurally cannot cover, its test asserts absence from the route **collection** rather than a 404, and it names its own deletion trigger in every file it occupies — because scaffolding without a named expiry is surface nobody has admitted to yet. Story **0021** adds `Components/` to the `app/Livewire/` grouping — the first subfolder that is neither a module area nor scaffolding, but a *reusable component* screens embed — and a new top-level section, [A `wire:ignore`d client-owned region — the app's first instance](conventions/base-standards.md#a-wireignored-client-owned-region--the-apps-first-instance): seed a client-owned subtree from server state exactly once, sync back at defined points only, and the load-bearing caveat that an unescaped `{!! !!}` echo inside such a region is safe only because of what the seeded value already is — sanitized elsewhere, on write — never because the region is client-owned. Story **0022** (the shared searchable, server-side-filtered multi-select) adds `Components/`'s second Livewire component (`SearchableMultiSelect`, plus the `MultiSelectOptionsResolver` interface a consumer implements — not a component itself), the first real class living directly under `app/Actions/` with no subfolder (`NormalizeForSearch`, confirming the "or directly under `app/Actions/` if it belongs to none" branch of the one-subfolder-per-area rule with an actual instance), and the first exception in `app/Exceptions/` that deliberately carries **no** `render()` (`UnresolvedSelectionException`, which must never reach the HTTP layer as a status code — unlike its three response-rendering siblings). It also adds a new top-level section, [Flux Free's `ui-dropdown` requires a real `<button>` trigger descendant](conventions/base-standards.md#flux-frees-ui-dropdown-requires-a-real-button-trigger-descendant--confirmed-twice-not-a-one-off) — confirmed twice now (story 0021's link popover, this story's results dropdown) that `flux:dropdown` throws on every page load when its trigger renders no real `<button>`, with the hand-assembled `x-show`/`x-cloak`/`click.outside` popover as the established fallback — and corrects a `tests/Browser/` file-count that had been wrong since before task 0018 (a missing `RolesIndexTest.php`, found incidentally while adding this story's own file).
- [Code style](conventions/code-style.md) — line-level conventions (explicit types, braces, shared validation traits, PHPDoc array shapes, per-method action injection and the constructor-injection exception for an action whose `__invoke()` signature is a public contract), each with a real ✅/❌ pair. Since task 0015b that exception also carries the constraint that falls out of it: **an action is resolved from the container, never `new`-ed, including in tests** — three actions gained their *first* constructor dependency in that story and every `new RequestEmailChange` call site broke at once, which is the proof that a zero-argument constructor is not a contract. Since story 0020 the `app()` carve-out is **stated as a rule rather than as its one instance**: it had read "a `#[Computed]` method may use `app()`", which described the only case that existed, and a Livewire `updated<Property>()` lifecycle hook — invoked through `wrap($component)->__call($name, $params)` with fixed parameters rather than a container `call()`, so a type-hint there is never resolved — fails the same test for the same reason. **A method whose parameter list is fixed by something other than this class may use `app()`, and nothing else may.** Story 0022 confirms the rule generalizes past Livewire's own lifecycle hooks: `App\Livewire\Components\SearchableMultiSelect::hasSearchedEnough()` is a plain public method, neither `#[Computed]` nor a lifecycle hook, called directly from its Blade view as `$this->hasSearchedEnough()` with a fixed, zero-argument call site — the *view*, not Livewire, is what fixes the parameter list here, and the existing rule's wording already covered it without needing a rewrite.
- [Naming](conventions/naming.md) — class/file naming (including `<Model>Policy` and its ability methods, and the `is<Thing>(self $x): bool` shape of a model's shared identity predicates), Livewire component ↔ view naming **and the `Index`-in-a-subfolder exception to it**, validation-trait naming, route naming, permission naming, translation keys (including why `RoleName`'s backing values are the persisted role names rather than lowercase), boolean property naming, the `trans_choice()` / `|`-delimited plural convention for a count-dependent message, and — since task 0013 — the rule that a lang file supplying copy for a **registry** mirrors that registry's keys exactly, so one identifier is simultaneously the config entry key, the translation leaf and the rendered `data-test` hook. Since task 0015a the boolean-naming rule states explicitly that it binds a `#[Computed]` boolean **method** as well as a property (which is what a modern Livewire screen actually exposes to its view), and adds the rule that a predicate must be unambiguous read *out* of its class — `isRecentlyConfirmed()`, deliberately not `isConfirmed()`, since recency is the whole content of the check while the class name already supplies "password". Task 0017 adds three things: a **third** row to the `Index`-in-a-subfolder table plus the property the first two could not demonstrate (the subfolder name is **kebab-cased** on the way down, so `App\Livewire\SalesRegions\Index` → `livewire/sales-regions.blade.php`) and the trap's other half — an `artisan make:` scaffold that deposits an unused nested stub which breaks nothing and simply sits there; the rule that a `<noun>Rules()` method's noun is the **field**, not the model, so a model-named validation trait holds `rateRules()` / `codeRules()`; and an explicit exception for a validation `attributes` block's **camelCase** leaf, which must equal the property name Laravel substitutes and is therefore not a snake_case-rule violation. Task 0018 turns one of this page's forward-looking sentences into a record: the registry-mirroring rule's multi-word clause has named `items.sales_regions` **by name** since task 0013 while every shipped registry key was a single lowercase word, and 0018 is the entry that had to choose — it went in as `sales_regions`, with the three identifiers that move together spelled out and the three kebab-case *values* in the same entry that correctly stay kebab because none of them is a registry key. It also adds the rule that **a `label()` method on an enum is not automatic**: story 0016 deferred `SalesRegionKind::label()` to "the first story that actually renders `kind`", 0018 is that story and declined it, keeping the two labels in its own screen's lang file — *add `label()` when a second consumer appears, not when the first one does*. Story 0019 claimed to turn a *second* of this page's predictions into a record — that the composed-label rule's "a catalog addition silently renders a raw key" had finally come true, `media` having shipped with no `roles.modules.media` leaf in either locale — **and story 0020 established that it never did**: both leaves are present and always were. The ⚠️ is corrected in place with what it used to say, and its two rules survive unchanged (add the leaf to both locales in the same change; the `roles.actions.*` half needs nothing, since a new module reuses the four existing verbs) — precisely *because* nothing fails when a leaf is missing, `__()` returning its own key is not an error condition, and the seeder tests assert names and counts rather than rendered copy, so the failure mode is visible only by looking at the screen. `media` is now the evidence the rule is followable. See [errors-log.md](errors-log.md#one-docs-pass-reported-two-gaps-that-were-not-there-both-marked-verified--2026-08-29). It also carries the updated `RolePermissionSeeder::MODULES` quote, `MediaValidationRules` as the fifth `<Noun>ValidationRules` trait and the first to hold **constants** beside its rule methods, `MediaPolicy` as the third follower of "name a permission once on the class that owns the rule" (leaving `UserPolicy`'s literals one outlier in four), and the `Media\Gallery` counter-example to the `Index`-in-a-subfolder exception — it resolves to the **nested** `livewire/media/gallery.blade.php`, because the exception keys on the class name and not on living in a subfolder. Story **0021** adds a second confirmation of that same counter-example: `App\Livewire\Components\WysiwygEditor` → `resources/views/livewire/components/wysiwyg-editor.blade.php`, unaffected by living inside `Components/`, a subfolder that is itself not a module area. Story **0022** adds a third: `App\Livewire\Components\SearchableMultiSelect` → `resources/views/livewire/components/searchable-multi-select.blade.php`, plus a note that the mirror rule governs a Livewire `Component` subclass and not every file under `app/Livewire/` — its sibling `MultiSelectOptionsResolver`, a plain interface with no view, is outside its scope entirely. This story adds no permission, policy or route (D7: the shell ships no `Gate::authorize()` call of its own), and its `NormalizeForSearch`/`UnresolvedSelectionException` classes follow the existing imperative-verb-phrase and `<Thing>Exception` rules with nothing new to decide.

## Testing

- [Testing index](testing/README.md) — how to write, review, and run tests in this repo: testing philosophy and anti-patterns, a QA risk-based thinking guide, Pest 4 backend conventions (including how to test the **four** real authorization layers — route middleware, permissions, policies, and, since task 0015a, **step-up authentication**, which no `Gate::forUser()` test reaches — why a `Livewire::test()` authorization test and an HTTP one are not substitutes for each other, and why a step-up layer's *exempt* cases must be tested as deliberately as its refusals, since an over-block is its failure mode), and CI commands/coverage. Start there; it links out to `.claude/skills/pest-testing/SKILL.md` for basic Pest syntax rather than duplicating it. Since story 0021 [ci/commands.md](testing/ci/commands.md) also records an **unscoped `php artisan test` fatal at PHP CLI's default 128M memory limit** on a host-native (no Sail) worktree — the `artisan test`/`vendor/bin/pest` sibling of its pre-existing PHPStan memory-limit note, with the exact workaround command and why it must invoke `vendor/bin/pest` directly (a subprocess launched by `artisan test` does not inherit its parent's `-d` flag) — and a nuance to its own "always targets `testing`" claim, reconciling it with [worktree-databases.md](testing/worktree-databases.md)'s host-native/Sail distinction rather than leaving the two pages reading as contradictory.
- [Frontend / browser testing guide](testing/frontend/README.md) — QA-oriented guide for browser-level tests: the tooling decision (Pest 4 browser testing, not a separate Playwright/BDD runner), the user-story → Gherkin → Pest workflow and reference prompt, setup status — since task 0006b the `tests/Browser/` suite is **wired up and running in CI on Chromium**, with cross-browser coverage and the browser-specific trigger policy still open — Gherkin guidelines + domain glossary, a browser-specific quality checklist, the frontend coverage policy, and worked scenario/test examples. Since task 0018 (the suite's **third** file, `tests/Browser/SalesRegionsIndexTest.php`) [playwright-setup.md](testing/frontend/playwright-setup.md) also owns two things a QA author otherwise meets by discovery: this repo's **waiting rules** — `->waitForEvent('networkidle')` banned outright (it never settles here, and one debugging session's repeated 15-minute hangs leaked ~60 `playwright run-server` processes and OOM-killed the MySQL container), a short bounded `->wait(n)` with a stated reason as the one accepted mitigation, and the instinct underneath both, *read the DOM's own `[wire:snapshot]` ground truth rather than waiting longer* — and a **selector ⚠️** for an admin list screen whose row controls are icon-only, where "prefer visible text" inverts and two specific assertion traps bite (a page-global `assertSee('0%')` matches inside `10%`; a disabled-state helper must match `disabled="disabled"`, never a bare `disabled` substring, which Flux's own `disabled:opacity-75` class carries on the **enabled** branch too). That page's folder-structure rule is also sharpened rather than repeated: it has said since task 0006 that the flat `UsersIndexTest.php` is debt and "the next browser test goes in its mirrored subfolder", the next browser test shipped flat as well, so the lesson now lands one step earlier — **a story file that names a test path is making a convention decision, and the path belongs in the Phase 2 review** — and story 0020 is the ✅ on that: its task file named `tests/Browser/Media/GalleryTest.php` explicitly, cited the rule, and the file landed there. Story 0020 also gives that page three source-verified tooling findings that change what a browser test can be written to do at all: **`->wait(n)` is not a polling primitive** (it is routed through a retry loop that only re-tries on a thrown expectation, so it gets none of the machinery an assertion gets) **and a longer wait can fail *because* it is longer** (`Playwright::$timeout` is 5000 ms, so `->wait(5)` throws against its own budget — a `->wait(2)` was widened to 5 on exactly that wrong theory, got worse, and was reverted), with a ⚠️ recording this repo's second honestly-documented flaky browser test and the three execution-backed attempts that did not close it; **a real file upload is unreachable through `visit()` here**, in two independent ways (`attach()` is refused for *any* file input over this plugin's non-collocated Playwright connection, and its own HTTP driver builds the Symfony request with a literal `[], // @TODO files...`, so a multipart body is never parsed into `UploadedFile`s) — making upload *completion* Feature-test territory permanently, while everything up to the XHR starting stays coverable; and **a page mounting one component twice duplicates every `data-test` hook**, tripping Playwright strict mode on single-element assertions while `assertSee()` silently tolerates it. Since story 0019 the index also points at [security/image-upload-processing.md](security/image-upload-processing.md) from **Related** rather than from a new Security heading it has never had — it earns the pointer because most of that page is test-design guidance a QA author would otherwise rediscover (assert a converted file's **byte signature**, never its extension; a read-only-directory fixture fails the *first* write and leaves a partial-write cleanup branch vacuously asserted; a `.jpg`-named blob of random bytes reports as `image/jpeg`, so `dimensions:` rather than `mimes:` is what rejects it) — and [ci/pipeline-integration.md](testing/ci/pipeline-integration.md) now records the first `setup-php` input added for **correctness** rather than tooling, `extensions: imagick`, together with why `->skip(fn () => ! extension_loaded('imagick'))` was rejected in its place: it would have turned the story's central acceptance criterion into a green tick asserting nothing, on all three matrix legs, invisibly. Since story 0021 the suite's **fifth and sixth** files, `tests/Browser/Components/WysiwygEditorTest.php` and `Components/WysiwygEditorOutputHtmlTest.php`, both mirrored (four of six files mirrored is now the majority, not the two-of-four minority a story ago), and [playwright-setup.md](testing/frontend/playwright-setup.md) gains a new subsection distinct from the `->waitForEvent('networkidle')` incident: **orphaned `playwright run-server` processes re-accumulate on every ordinary browser-test run in this environment**, not only after a hung call, with the mitigation (`pkill -9 -f "playwright run-server"` after any browser-test session) recorded as a Phase 5 finding that root-caused a doubled flake-rate measurement as environmental rather than a code regression. Since story 0022 it also owns [a DOM-occlusion diagnostic](testing/frontend/playwright-setup.md#a-hung-click-with-no-error-anywhere-check-for-occlusion-with-documentelementfrompoint-before-suspecting-a-timing-flake): a real Playwright click hanging on its own actionability timeout with no error anywhere in the stack can mean a sibling element is silently covering the target rather than a broken handler or a genuine timing flake — `document.elementFromPoint()` at the target's real coordinates is the fast way to tell them apart, a diagnostic `Livewire::test()` structurally cannot run. The suite's browser-test count moves to eight files (`tests/Browser/Components/SearchableMultiSelectTest.php`), and that same pass corrected a stale under-count in [playwright-setup.md](testing/frontend/playwright-setup.md#folder-structure) that had omitted `tests/Browser/RolesIndexTest.php` (present since task 0011) from every file-count claim since before task 0018.
- [Worktree databases](testing/worktree-databases.md) — why `.env.testing` is required per checkout (a missing one lets `--env=testing` silently fall back to the real `.env` and hit the dev database — see the 2026-08-26 errors-log entry) and why each `git worktree` needs its own testing database name (`testing1`, `testing2`, …) rather than sharing one, plus setup/cleanup steps.

## Security

- [Security knowledge base](security/README.md) — durable security rules established by `appsec-auditor` during Phase 4 audits, each with a real code example from this repo. **Eleven** pages so far — this said "Ten" while listing nine of the eleven that now exist, the missing ones being [model-instance-trust.md](security/model-instance-trust.md) (task 0017) and [image-upload-processing.md](security/image-upload-processing.md) (task 0019's Phase 4 re-audit), summarised at the end of this entry: [authorization patterns](security/authorization-patterns.md) (what the Super Admin `Gate::before` bypass does and does not cover, permission-cache flush ordering around a transaction, always passing the guard to `hasRole()`, `Gate::before` closures guarding with `instanceof`, why `config($key, $default)` alone cannot cover a present-but-`null` key, why an ability must cover every **attribute** that achieves its effect rather than only the operation it is named after, and — from task 0008 — why a model-level guard reading a row's protected identity must separate "column not hydrated" from "hydrated but null" with `array_key_exists` rather than `??`, plus why role-name acquisition is closed by the `creating`/`updating` guards rather than by the unique index alone, and — from task 0008a — why a rule that must bind a **Super Admin actor** has to be a direct `throw` instead of a `Gate` check, why such a guard must check the target's *current* state and not only the value being submitted, and why authorization that consults an Eloquent relation must reload it *above* the first check that reads it, and — from task 0010 — why an identity derived from a **mutable column** must be locked at the model layer as soon as code exists that can mutate it, why the lock must cover exactly the identity and not the row's whole surface, and why more than one guard transforming the same full-replace payload must agree on what an *omission* means, and — from task 0011, the view half of that same screen — why a control **omitted from the DOM** is safe only for the one value whose guard preserves an omission, why the omission must be a single filter applied *before* the grouping transform rather than a per-item branch inside it, and why a regression test for it must assert a rendered **count** rather than a presence, and — from task 0012 — the **confirmed-safe** guarantee every later module gate inherits: a `can:`-gated route's 403 names no permission because `AccessDeniedHttpException` *is* an `HttpException`, so the debug renderer is structurally unreachable and `errors::403` prints only the generic message byte-identically at both `APP_DEBUG` settings — with the two things that *would* reopen it named (an app-owned `errors/403.blade.php`, or a `Response::deny('…')` naming an ability), plus the ⚠️ on the pre-existing cache-flush rule recording that the flush a `DB::transaction()` wrapper relocates is usually the **vendor's**, invisible in the diff, and — from task 0013 — the first rule governing a **declarative permission registry**: a registry that means "ungated" by *absence* fails open silently, because `empty()` is the one construct that reads a missing key without a warning, so an omitted key, a misspelled key and a `null` all render an entry for everyone while the *wrong-looking* mistakes all fail closed — **closed in the same story**, with both ✅ blocks now the shipped guard tests (an allow-list a new ungated entry cannot join without editing its one literal line, and a test set-equating every entry's `permissions` with its route's real `can:` middleware) and the note that asserting only `toHaveKey('permissions')` is a correct narrowing rather than a gap, since every other registry key either raises an `ErrorException` at render or fails closed; plus a companion **confirmed-safe** section recording the six `Gate::any()` mechanics a later epic should not re-derive, and — from task 0015 — why a **rate limit keyed on the target alone becomes an attack on the target** the moment a second, cross-user caller exists, with the table showing that neither obvious fix works without the other, and the four decisions in the shipped two-limiter shape: check the narrower key first because `RateLimiter::attempt()` consumes on success, fall back to `'unauthenticated'` and never to the target's own key, exempt the target's **own** request from the aggregate ceiling, and give the surviving control its own test whenever an exemption removes a backstop; plus a **confirmed-safe** addendum on the relation-reload rule, now that this repo's own audit-log capture makes a *second* caller pre-hydrate `roles` one statement above the action — safe only because `UpdateUser` uses `load()` rather than `loadMissing()`), [seeder safety](security/seeder-safety.md) (why `db:seed` is production-reachable here, environment allow-lists over deny-lists for fixture data, and the rules for bootstrapping a privileged account from a configured email address), [signed-link verification patterns](security/signed-link-verification.md) (why `ValidateSignature` is globally prioritised ahead of `SubstituteBindings`, why a value bound into a link by `sha1()` must be normalised first, why a locked re-check still needs the unique index to have the last word, and why every refusal branch must be indistinguishable), and [Livewire component authorization](security/livewire-authorization.md) (the verified `PersistentMiddleware` allow-list and what it silently drops on `/livewire/update`, gating every method that mutates *or discloses*, why `#[Locked]` is what keeps `Rule::unique()->ignore()` safe, and why a rule enforced only in a component is bypassed by every other call site of the action — a gap task 0004 recorded on this screen and task 0008a closed, with the before/after kept because the shape recurs; since task 0015 the "mutates **or discloses**" rule finally has its shipped example, the Users screen's three previously-ungated modal openers, including why a disclosure gate asks a **stronger** ability than the write it precedes, why the component performs **no** tier lookup to get there, why the one branch that exists is an identity check and `confirmDelete()` has none, and why `$users` being `#[Locked]` is a change to every test that wrote it; and since task 0015b the **observability half** of that same rule — gating a method and knowing when the gate fired are two different properties, and on a component reached over `/livewire/update` the second matters more, because a refused fiftieth probe and a successful save are the same `POST` in the access log: the ✅/❌ pair against the bare `Gate::authorize()` every one of these sites carried until then, why the helper is **method**-injected here while the actions behind the screen constructor-inject it, why `mount()` is the one deliberate exception — this page's own allow-list table read in the *opposite* direction from the `password.confirm` row above it, `can:`'s **presence** on `PersistentMiddleware` making a log there unreachable over HTTP where `RequirePassword`'s **absence** forced a guard into the method — and why a disclosure gate still needs its state assertion, since `Log::spy()` cannot distinguish a gate that ran before the assignments from one that ran after; and since story 0020 the page's **opening sentence is corrected** — it scoped the whole document to a *"full-page Livewire component"*, true of everything audited through task 0018 and false as of the first **embedded child** with no route of its own — plus the section that distinction produces, [the routeless case](security/livewire-authorization.md#the-routeless-case-a-component-with-no-route-has-no-per-request-backstop-at-all): why `mount()`-only gating is exactly right for a routed screen (`can:` **is** on `PersistentMiddleware` and is replayed on every round trip) and leaves a routeless one unguarded from its second request onward, the ❌/✅ pair from finding F-1, and the three rules it yields — **enumerate methods, not abilities**; a `#[Computed]` property **is** a method; and **a render-path gate fails closed while an action-path gate throws**, since a throwing `#[Computed]` propagates out of the child and 500s the *host* page, which is the failure the consumer's `@can` wrapper exists to prevent, arriving one layer lower; and since story 0022 the page's ["every server-derived property is `#[Locked]`" section](security/livewire-authorization.md#every-server-derived-property-is-locked-not-just-the-ids) gains a ✅ for `App\Livewire\Components\SearchableMultiSelect`'s **seven** locked properties, the pattern's next real instance and the one that broadens what "server-derived" covers — three of them (`$minSearchLength`, `$debounceMs`, `$resultLimit`) are not server-*computed* at all but a **consumer-set-once configuration value**, with `$resultLimit` the sharpest case since a client-writable value there would defeat D9's whole bounded-fetch contract against an 8,100-row resolver; and the page gains a new section, [A save-time gate and its display-only twin must share one resolution method, or the two will drift](security/livewire-authorization.md#a-save-time-gate-and-its-display-only-twin-must-share-one-resolution-method-or-the-two-will-drift), for Phase 4 finding F-2 — the component ships **no** `Gate::authorize()` call of its own (D7), so this is the page's first pattern about two checks (a display refresh and a save-time gate) answering one yes/no question with no `Gate` anywhere in it, explicitly cross-referenced as the non-`Gate` sibling of `architecture/authorization.md`'s `canEdit`/`canDelete` UI-hint rule), [soft-delete patterns](security/soft-delete-patterns.md) (why the `SoftDeletingScope` on an authenticatable *is* the sign-in refusal with no second check behind it, the vendor passkey relation that is the one place that scope can be lost silently, why freeing an identifier means revoking everything keyed by its string, how adding `SoftDeletes` silently stopped Spatie detaching role grants, and why a deterministic placeholder written into a `UNIQUE` column still needs the `23000` catch), and [CI workflow hardening](security/ci-workflow-hardening.md) (the rules for `.github/workflows/*.yml` established by task 0006b's first CI step that downloads and executes a third-party binary: why SHA-pinning covers `uses:` but not `run:`, why `npx <pkg>` must fail rather than silently fetch `@latest`, why `npm ci` is what makes the lockfile an actual pin, why third-party install steps must run *above* the step writing Flux credentials to disk, why git-ignoring `tests/Browser/Screenshots` is only sufficient while nothing uploads artifacts, and the verification that `DB_DATABASE=testing` still covers the new `Browser` suite), and [Blade / Livewire output encoding](security/blade-livewire-output-encoding.md) (where `{{ }}` stops being enough, from task 0006's Users list markup: why a value interpolated inside a `wire:*` directive lands in a **JavaScript** evaluator that the HTML parser hands the decoded quote back to — making `@js()` the only correct encoder for a directive argument — why a public property with no `wire:model` is still client-writable unless `#[Locked]`, why a modal must read authoritative values from the model rather than back them out of a client-writable array, and the template patterns verified already safe), and [login-time account-status enforcement](security/login-status-enforcement.md) (from task 0007, which turned `users.status` into an authentication control: why every `Inactive` → `Active` transition is now a privilege grant and why the pre-save value a `Verified` listener needs is `getPrevious()` and never `getOriginal()`, the four vendor `$guard->login()` call sites mapped to the three enforcement points that reach them, why a custom `Fortify::authenticateUsing()` callback must resolve credentials through the guard's `UserProvider` to keep the soft-delete scope and the password rehash, why the refusal must come only *after* credentials verify and must never name the status, why a blocked attempt counts toward the limiter only because `fortify.limiters.login` moves throttling onto the route, why rejecting on the `Login` event alone is undone by `SessionGuard::login()`'s very next line, and why `Passkeys::authorizeLoginUsing()`'s user parameter must be nullable), and [step-up authentication](security/step-up-authentication.md) (from task 0015a, the app's **third** authorization layer — a password-freshness guard answering "is the person at the keyboard still the account holder", which route middleware and policies both pass for a hijacked or unattended session: why the check must reuse `RequirePassword`'s own session key, config key and `>` boundary rather than re-derive them, and why that boundary needs asserting from *both* sides to tell `>` from `>=`; why the throwing guard must be a **wrapper** around the non-throwing predicate the UI warning reads, so the hint cannot drift from the rule; why it runs strictly **after** every `Gate::authorize()` on its branch — an inverted ordering turns a permission refusal into a credential prompt plus a target-exists disclosure, and a branch with no preceding `Gate` call is not an exemption; why the refusal is a **direct throw** (a `Gate`-mediated one is inert for the Super Admin it most needs to bind) rendering **423, never 403**, with `Handler::render()` verified to consult `render()` ahead of the debug renderer and the rule that the thrown message stays a constant; and why the guard must key off the **narrowest** booleans describing the privileged write — the story's own Phase 3 shape got this backwards for a third-party email change, closed by decision D7. Plus a **confirmed-safe** list of six verified mechanics an later story should not re-derive, a **closed-by-decision** section covering the first Phase 4 audit's four findings as ❌/✅ pairs, and a ⚠️ list of the two doors the layer still does not close — `settings/security` and `settings/profile`), and [model-instance trust](security/model-instance-trust.md) (from task 0017, and the first page here about a **domain invariant** — a rule about the shape of the data, "exactly one Sales Region is the default and it is always active", rather than about who may act. Every "derive the state, never accept it" rule on `authorization-patterns.md` is stated in terms of a *parameter beside* the model; this is the same failure class one level down, where the untrusted input **is** the model. Two rules, one root cause and one remedy: a guard must **re-read its subject under lock inside its own transaction** — `SetDefaultSalesRegion`'s refusal is correctly placed inside the transaction and still cannot hold, because the attribute it tests was read before that transaction existed, with the three reasons `lockForUpdate()` closes none of it and three confirmed paths to the forbidden state, the sharpest needing no forged input at all; and `save()` **writes the whole dirty set, not the `fill()` allow-list**, so "the single named writer of this column" is a convention among callers rather than an enforcement — a caller who dirties `is_default` before calling `SetSalesRegionActive` reaches two defaults without the one class that exists to prevent it ever running. Both closed the same day, with the note that `App\Actions\Users\UpdateUser` shares the second shape and is recorded as known-and-open rather than newly discovered. A third section records what re-auditing that fix as new code found the next day — the round-1 fix's own lock-ordering docblock justified a scenario that provably cannot occur while round 1 had *introduced* a real, two-live-MySQL-sessions-confirmed deadlock elsewhere (R-1), and the promotion branch could return an instance that lied about `is_default` (R-2) — plus the recorded-not-fixed note that the `Gate` target is still the caller's instance (R-3) and an authorization-ordering fix that deliberately did **not** touch an already-accepted two-transaction shape (R-4). Two of its own claims were then corrected by the Phase 5 code review a day later, which is the audit-authored-page failure mode recurring with a **one-day** fuse and being caught: F-1 *is* dashboard-reachable, and the residual deadlock window inside `SetSalesRegionActive` is closed by the outer transaction's `attempts: 3` retry rather than by lock ordering)), and [image upload & server-side image processing](security/image-upload-processing.md) (from story 0019's Phase 4 audit and re-audit — the first page here about a feature that accepts a **file** from a user and then *decodes* it, and the first written as ❌/✅ pairs describing the shipped state from the outset rather than after a later pass caught drift. Its central rule is that **a pixel-dimension cap does not bound decode memory**: bytes-per-pixel is a property of the installed ImageMagick build, not a constant, and this project's Q16-HDRI build measures ~35 bytes/pixel — so an 8000×8000 PNG that is **7,898 bytes on disk**, 0.1% of the 8 MB size cap, extrapolates to ~2.2 GB in a single synchronous request, with `memory_get_peak_usage()` reporting a few MB throughout because it cannot see Imagick's C-level allocations. The decoder must therefore enforce the same ceiling the validator promises, derived from the **same constant** so the two cannot drift. It carries the measured evidence that the two limit layers do *different* jobs and neither is redundant — `WIDTH`/`HEIGHT` refuse at header-parse time at 0.00 s and 38.5 MB, while the byte ceiling is slower and coarser but is the only layer that sees an animated input whose *per-frame* dimensions are all legal (965.9 MB with limits versus 2,823.1 MB without, which matters because `decodeAnimation` is `true`); why `DISK => 0` is what makes the byte ceiling bite instead of converting memory exhaustion into disk exhaustion; and a ⚠️ that `Imagick::setResourceLimit()` is **process-global**, which is not a concurrency hazard on this NTS, Octane-free stack but *is* sequential contamination — harmless only while `GenerateImageConversions` remains the single Imagick consumer in `app/`. On the storage side: why `Storage::putFile()`'s content-sniffed extension must never name a file the web server serves directly (a sniffed `image/svg+xml` becomes `.svg` — stored XSS from the app's own origin), the allow-listed-`match` replacement, and the **enumeration** that proves that `match`'s `default => throw` arm unreachable rather than asserting it: over Symfony's full 1,848-entry table exactly three MIME types can satisfy `mimes:jpg,jpeg,png`, and the one the map does not cover (`image/pjpeg`) is unproducible by either detector in this stack. It also records that **`image` + `mimes:` is not by itself a content check for a Livewire temporary upload**, because `TemporaryUploadedFile::getMimeType()` routes through Flysystem's `FallbackMimeTypeDetector`, which guesses from the *extension* whenever finfo is inconclusive — 2 KB of `random_bytes()` named `garbage.jpg` reports as `image/jpeg`, and `dimensions:` is the rule that actually rejects it; that `Storage::put()` **returns `false` rather than throwing** on a `'throw' => false` disk, so an ignored return commits a row pointing at a file that was never written; and — the test-design half of that same finding — that a read-only-directory fixture makes the *first* write fail, leaving the partial-write cleanup branch **vacuously** asserted, with the `mkdir()`-over-the-second-path fixture that actually exercises it. Finally: why an imaging exception must never reach the user (Imagick messages carry absolute paths and internal source locations), why the catch must be on the base `ImageException` (a resource refusal arrives as `ImageDecoderException` on one path and `DriverException` on the other), and a **confirmed-safe** list of six verified mechanics — including the rule that a limiter key falling back to a shared `'unauthenticated'` literal must be *proven* unreachable, the companion hazard to a limiter keyed on the target).

## Decisions

- [Decision records](decisions/README.md) — ADR format and folder purpose. First ADR recorded: [0001 — UUID primary keys](decisions/0001-uuid-primary-keys.md) (UUIDv7 via `HasUuids` for `users` and PRD Epic 2/4 domain entities). **Amended by story 0019** ([Amendment 1](decisions/0001-uuid-primary-keys.md#amendment-1-2026-08-27--the-scope-is-the-policy-not-the-list-of-seven)), which closes the follow-up [database/schema.md](database/schema.md#notes) had recorded as deferred since task 0016: `media` is the **second** UUIDv7 table the ADR's original seven-entity list does not name, and a second silent contradiction is this doc set's own recurring stale-claim failure mode — so the amendment states the **policy** (every new business entity is UUIDv7, with one named `bigint` exception for a high-volume internal geography lookup table) rather than extending an enumeration a third story would extend again. It also records honestly that the ADR's *stated* rationale, enumeration safety, transfers to `media` (ids interpolated into Blade across a gallery of tiles) but not to `sales_regions` (a fixed public country catalog has nothing to enumerate — that key is consistency-driven).

## Errors log

- [Errors log](errors-log.md) — structured record of real mistakes and the rule adopted to avoid repeating them. Thirty-four entries, the newest two both from story 0022's real-browser testing (its component-level Feature suite stayed green through both): an absolutely-positioned results dropdown with no explicit `top`, written before the chip row in source order, silently occluded the chip area's remove controls and hung a real Playwright click on its own actionability timeout with no error anywhere — found with `document.elementFromPoint()`, fixed by reordering the markup rather than adding a `top` value; and a `wire:model.live.debounce.{{ $debounceMs }}ms`-style attribute cannot be interpolated at all, because Blade's `ComponentTagCompiler` parses a component tag's attribute *names* with a regex that excludes `{`/`}`/`$` — a different, previously-undocumented failure from the three attribute-*value* Flux/Blaze traps below it, caught at Phase 2 review before real code was written, notable because the identical shape had already been silently worked around twice (`Gallery`'s hard-coded `300ms`, `WysiwygEditor`'s Alpine `setTimeout()`) with neither prior file stating why. Before those: a **third** Flux/Blaze compile trap sharing one root cause with the two below it — a bare `@disabled(...)`/`:disabled="..."` written directly inside a `<flux:button>` tag's attribute list corrupts the whole compiled view into invalid PHP rather than merely mis-rendering one control's state, found by `code-reviewer` via direct `Blade::render()` execution while building story 0021's toolbar (the rule: branch the whole tag with `@if`/`@else`, never a conditional attribute, on any `<flux:*>` tag). Before it: a docs pass that reported a gap, got it fixed, and then shipped its own report of the gap — story 0019's Phase 6 wrote *"`LogRefusedPrivilegedAttempt` appears nowhere under `app/{Livewire,Actions}/Media/` — verified by grep, not assumed"* into two pages while the code it describes imports the class and calls it twice, and it stood for a whole story (the rules: **a docs pass owns re-verifying, before the story closes, any gap it reported and got fixed** — a successful finding invalidates the sentence that reported it and nothing else will; re-run the *literal command* a doc claims to have run, on every "verified by" claim the pass itself wrote; and **never date a doc's claim by commit order on a workflow that rebases**, since the fix here was rebased into the *earlier* commit and `git log` therefore argued the false doc was the later word — with the added tell that "verified by grep" made the wrong claim *more* trusted, the inverse of the hedge entry below); four added 2026-08-28 by tasks 0019a/0019b and their Phase 4/5 follow-up — an Imagick resource-limit unit mismatch masked by the host's own `policy.xml` silently clamping an over-large request, a self-inflicted false lead from checking a container's user via the wrong `sail` invocation, an open, unresolved observation about full-suite `--parallel` runs degrading under sustained session load, and a stale `public/hot` file that made all 19 browser tests time out with no code defect at all — see [errors-log.md](errors-log.md) for all four): a `wire:click` attribute converted from `{{ Js::from(…) }}` to `@js(…)` "for consistency" that silently made every row toggle on a shipped screen a no-op — no PHP error, no console error, no failed request, and invisible to `Livewire::test()->call(...)`, which never goes through a compiled `wire:click` at all (the rules: prefer `{{ }}` echoes for a dynamic value in a component-tag attribute, and **verify the compiled output rather than the absence of an error** before trusting an attribute-syntax cleanup) — **whose own recorded root cause did not survive the next day's verification and now carries a dated correction**: the stated discriminator ("one `@js()` compiles, two do not") reproduces in neither direction, the component rather than the count is what matters, and what actually killed the screen is left explicitly open rather than replaced with a second confident guess; three consecutive verification records that each named **two** of this project's three quality gates and were read as records of all three — Larastan level 7 was never run against story 0017 until Phase 5's code review ran it and found two real errors in the component's own `save()`, three phases past implementation (the rules: a verification record names all three gates and the result of each, including "not run"; **an omission is invisible in a record of positives**, so a checklist is enumerated by the checklist rather than reconstructed from the report; and a reviewer's cheapest opening move is to diff the phases' records against the gate list — the two gates that *were* run are the two with loud per-file feedback loops, and the missing one is the one nothing else prompts you to run); a missing `.env.testing` letting `artisan migrate:fresh --env=testing` silently fall back to the real `.env` and wipe the shared development database, twice in one day, across two sessions (the rule: verify a `--env=`/`--database=` flag's fallback path, not just its happy path, before trusting it to isolate anything — now closed by a per-checkout `.env.testing` and a documented per-`git worktree` database-naming convention in [testing/worktree-databases.md](testing/worktree-databases.md)); a scope exclusion phrased in terms of **screens** while the story's unit of change was a **class** those screens share — `App\Actions\Users\RequestEmailChange` is reached by both the in-scope admin screen and the explicitly-excluded `settings/profile`, so adding an unthrottled log call to it handed every authenticated user an unbounded log-write primitive at zero cost (the rules: resolve an exclusion against the *files* the story will edit and state it in terms of classes; adding an unbounded side effect to shared code is a capability grant to its **least**-privileged caller; and prefer *bounding* the side effect over *excluding* the caller, since an exclusion re-splits one rule into two implementations); a code review's own "correction" that replaced an accurate docblock explanation with a wrong one — a finding hedged as "likely wrong" acted on without anyone running the one command that would have settled it, then verified and reverted by a later review pass (the rule: **a hedge is a flag that nobody ran the code, not a lower-confidence fact to trust anyway** — verify by execution before writing a fix around it, and watch for this specifically when a review's fix is to *documentation or a comment*, where no test can catch a wrong sentence); a deferred story whose findings were claims about a tree that no longer existed — six of eleven wrong after five intervening stories, including one that asked to fix a method those stories had deleted and one whose prescribed patch would have **reintroduced a bug already in this log** (the rules: re-verify every deferred finding against `HEAD` before Phase 3 and record each disposition including "already closed"; write a finding as the *property that must hold*, never as the patch, because a patch is only valid against the tree it was written on; and when a deferred instruction contradicts `docs/`, the docs win and the instruction is withdrawn in writing); a `DB::transaction()` wrapper added for an unrelated atomicity finding that silently moved a permission-cache flush **nobody had written** — the vendor's own, fired from inside `syncPermissions()` and a model event — from after the commit to inside it, reopening a fail-open window against a rule this repo's own security page already documented with the correct code (the rule: wrapping existing code in a transaction is a change to every side effect that code already performed, and the diff will not show you the one that moved); a count-based assertion over rendered HTML that counted a wrapper element it never meant to include — an unscoped `substr_count($html, 'value="')`, and then a `<ui-checkbox` prefix silently absorbing `flux:checkbox.group`'s own `<ui-checkbox-group>` (the rules: scope by a string that cannot match a sibling or wrapper, include the delimiter that ends an element name, and prove the count can move before trusting it — an over-count by a constant reads as the true number); both of this project's per-change quality gates being scoped by default and both silently reporting success — `pint --dirty` no-ops once a tree is committed, and `test --filter` never observes what a change did to the rest of the suite (the rule: run each gate **unscoped** before declaring the work done, and treat a story that registers a model event, observer, global scope or middleware as having whole-suite blast radius by construction); a test case planned against `verified`, a middleware that refuses nobody in this app because `User` does not implement `MustVerifyEmail` (the rule: prove a middleware can refuse *someone* before asserting that it refuses, and check the doc that owns the question first); a guard that accepted, as a caller-supplied parameter, the very state it existed to protect — so the fix for one finding reopened the same class of hole one level up, across three consecutive audit rounds — together with the full-replace-`sync*()` trap that started it (absence in a payload from a partially-visible form is not a decision) and the rule that when preserve-vs-deny is a product judgement, you escalate rather than pick; a security page that documented the **vulnerable** code as current, because it was authored during the Phase 4 audit that found the flaw and never revisited after the same story's fix landed (the rules: treat an audit-authored page as unsynced by default and re-verify its code quotes at Phase 6, and write a vulnerability up as a ❌/✅ pair from the start so the fix has a slot waiting for it); a three-round security audit in which two rounds found the flaw in the *previous* round's fix (a `Gate::authorize()` placed above the relation reload it depended on, and a Super Admin refusal that checked only the submitted role and never the target's current one) — with the rules that a fix is new code to be audited as such, that a relocated guard's new owner must be re-examined for every privileged value it can now write, and that ordering is part of a guard rather than an implementation detail; a Pest `arch()` rule given an array of namespaces that shipped green while structurally unable to fail on half its scope (`expect([...])` is disjunctive — one rule per namespace, and prove every architecture test can actually fail), a listener reading the pre-save `email_verified_at` with `getOriginal()` — which `save()`'s own `syncOriginal()` had already overwritten — while its sibling guard treated an administrator's *deactivation* as an activation trigger (the rules: `getPrevious()` is the post-`save()` pre-change value, and a column that starts denying access needs every write to it re-audited as a privilege grant), a task file's relative links breaking silently when it moves from `ai-spec/tasks/` to the deeper `in-progress/`/`done/` (found and fixed across six already-`done` task files; now a mandatory check on every [workflow.md](workflow.md#link-integrity-check-on-every-stage-move) stage move), a `null`-valued Livewire property bound to a native `<select>` that desynchronised the browser's own selection state and silently swallowed the user's pick (the rule: a `wire:model`-bound property must never be `null` — use an empty value in the type the DOM expects), the two Flux/Blaze traps found building the per-row disabled actions (a `tooltip` prop that counts as *present* whenever it is written on the tag, so it cannot be bound conditionally, and a `disabled:cursor-*` utility that never renders because `disabled:pointer-events-none` removes the button from hit-testing), two agents dispatched in parallel that both wrote to the same Blade view (one transiently, as a regression-proof), a deleted user's freed email address that kept its `password_reset_tokens` row live (an account-takeover path on whoever claimed the address next), the Gherkin actor/single-action convention violation, the Super Admin bootstrap treating an existing `users` row as proof of mailbox ownership (now half-closed by the pending-email mechanism), the redundant `users_uuid_unique` index left behind by the UUID conversion, a seeder test that depended on the developer's ambient `SUPER_ADMIN_EMAIL`, and three copies of a "this app has no Policies yet" claim that outlived the code by two tasks — with the rule against bare negative claims in docs that don't own the subject.
_Last updated: 2026-08-31 — Story 0022 (Shared searchable, server-side-filtered multi-select component). Like its two component-story predecessors it ships **no schema, permission, policy or route** — D7 states outright that the shell ships no `Gate::authorize()` call of its own, authorization belongs entirely to a future consumer's resolver — so this pass is almost entirely about the component itself and what two real-browser findings (invisible to the fully-green component-level Feature suite) taught. **Errors log** grows from thirty-two entries to thirty-four with a genuinely new class of bug (an absolutely-positioned dropdown with no explicit `top` silently occluding a sibling by DOM order, hanging a real Playwright click with no error anywhere) and a previously-undocumented Blade compile limit (a Livewire directive modifier's duration cannot be interpolated into a component tag's attribute *name*, a different failure from the three attribute-*value* Flux/Blaze traps already logged) — the second finding closes a gap two earlier components had silently worked around without ever writing down why. **Security** ([livewire-authorization.md](security/livewire-authorization.md)) gains a ✅ for the pattern's next `#[Locked]` instance — the first to lock a **consumer-set-once configuration value**, not merely server-computed display state — and a new pattern, [a save-time gate and its display-only twin must share one resolution method](security/livewire-authorization.md#a-save-time-gate-and-its-display-only-twin-must-share-one-resolution-method-or-the-two-will-drift), the first on that page about two checks agreeing on one yes/no answer with **no** `Gate` anywhere in the picture. **Base standards** gains a new top-level section confirming `flux:dropdown` requires a real `<button>` trigger descendant (now verified twice, not a one-off), the first real class living directly under `app/Actions/` with no subfolder (`NormalizeForSearch`), the first exception that deliberately ships **no** `render()` (`UnresolvedSelectionException`), and a corrected `tests/Browser/` file count that had silently undercounted since before task 0018 (`RolesIndexTest.php`, present since task 0011, was missing from every prior count). **The task file itself** ([`ai-spec/tasks/in-progress/0022-searchable-multi-select-component.md`](../ai-spec/tasks/in-progress/0022-searchable-multi-select-component.md)) needed two in-place Phase 6 corrections neither Phase 4 nor Phase 5 caught: D9's debounce mechanism and D10's `flux:dropdown` mechanism were both specified in Phase 1 and both proved unshippable in Phase 3, replaced by the same hand-rolled Alpine popover `wysiwyg-editor.blade.php` had already established — and OQ-7's ARIA scope is corrected from an intent statement into an honest shipped-state record: real label/`aria-label` association shipped, arrow-key navigation and an `aria-live` announcement region did not, both casualties of the same forced `flux:dropdown` deviation._

_Previously: 2026-08-31 — Story 0021 (Shared WYSIWYG rich-text editor component — frontend): a component with no page of its own and no schema, permission, route or migration to show for it — the properties that shape this pass are the four Phase 6 items its own task file left explicitly for `docs-keeper` rather than closed in Phase 5, plus what a docs pass finds on its own reading the diff.

**Two of the four were security/testing findings deferred, not forgotten.** `code-reviewer`'s Phase 5 review found (N4) that a bare `@disabled(...)`/`:disabled="..."` written directly inside a `<flux:button>` tag's attribute list — the toolbar's eight controls each needed one for the component's `$disabled` read-only flag — corrupts the **whole** compiled view into invalid PHP, reproduced live via `Blade::render()`; every button ships as a full `@if`/`@else` pair of separately-compiled tags instead, and [errors-log.md](errors-log.md#a-bare-disableddisabled-inside-a-fluxbutton-tags-attribute-list-corrupts-the-whole-compiled-view--2026-08-31) records it as the family's **third** documented instance, growing the log from thirty-one entries to thirty-two. The same round's B3 root-cause (a doubled flake-rate measurement traced to a stale `public/build/` bundle and ~50 leaked Playwright processes, neither a code regression) left two environment findings explicitly for this phase: **F5**, an unscoped `php artisan test` fatalling at PHP CLI's default 128M memory limit on this host-native worktree — now in [ci/commands.md](testing/ci/commands.md#environment-note-php-memory-limits-phpstan-and-php-artisan-test), the PHPStan memory note's new sibling, with the exact command and why it must invoke `vendor/bin/pest` directly rather than `php artisan test` (a subprocess does not inherit its parent's `-d` flag) — and **F6**, orphaned `playwright run-server` processes that re-accumulate on **every** browser-test run in this environment rather than only after a hung call, now in [playwright-setup.md](testing/frontend/playwright-setup.md#orphaned-playwright-processes-re-accumulate-on-every-browser-test-run-in-this-environment) with its `pkill -9 -f "playwright run-server"` hygiene rule.

**The third was a documentation contradiction, not a code finding.** N7 flagged that [worktree-databases.md](testing/worktree-databases.md)'s own Phase 3 correction (host-native `php artisan test` needs an explicit `DB_DATABASE=testing<N>` prefix because nothing pre-sets it before PHPUnit boots) and [ci/commands.md](testing/ci/commands.md)'s pre-existing "every run, local or CI, always targets `testing`" line had come to read as contradicting each other. Reconciled rather than merged into one blanket claim: CI (a job-level `env:`, verified against `.github/workflows/tests.yml`) and a host-native worktree with no override both land on literally `testing`, exactly as the page said — but a **Sail**-based worktree's container environment may already carry a different `DB_DATABASE` before PHPUnit ever boots, which the original sentence did not distinguish and now does.

**The fourth is architectural rather than a deferred finding, and it is the one this story actually adds.** `App\Livewire\Components\WysiwygEditor` is `Gallery`'s **first real consumer** and this app's **second** routeless gated component — [api/routes.md](api/routes.md) gains a full subsection alongside `Gallery`'s own, and [architecture/authorization.md](architecture/authorization.md#a-routeless-livewire-component-has-no-per-request-authorization-backstop) / [security/livewire-authorization.md](security/livewire-authorization.md#the-routeless-case-a-component-with-no-route-has-no-per-request-backstop-at-all) each gain a ✅ recording that story 0020's pattern held on its second real test, with one real finding rather than zero (`openGallery()`/`insertImage()` shipped ungated, fixed the identical way as `Gallery`'s own Phase 4 fix). It also **corrects the rationale story 0020's own D2 states** for why two galleries never cross-wire — verified against `vendor/livewire/livewire/dist/livewire.js`, the mechanism is listener-**name** uniqueness, not "DOM bubbling plus `#[On(...)]`" — and it introduces this app's **first `wire:ignore` region**, now its own convention in [base-standards.md](conventions/base-standards.md#a-wireignored-client-owned-region--the-apps-first-instance): seed once from server state, sync back at defined points only, and an unescaped `{!! !!}` echo inside it is safe only because of what the seeded value already is (sanitized elsewhere, on write) — a hard dependency verified closed today across all four named consumer stories (0027, 0061, 0077, 0079), not an open gap. [`dev.media-gallery-harness`](api/routes.md#devmedia-gallery-harness--temporary-environment-gated-scaffolding) is extended rather than replaced, now hosting two `WysiwygEditor` instances alongside its two `Gallery` ones, and its "deleted by 0027" comment names both 0020 and 0021.

**Verified as unchanged rather than assumed:** the permission catalog, seeded roles and policy count (no new permission, role or policy — `WysiwygEditor` reuses `MediaPolicy`'s existing `viewAny` ability); [database/schema.md](database/schema.md) and [database/migrations.md](database/migrations.md) (no model, column, table or migration in this story's diff, matching how tasks 0017/0018 and story 0020 before it were handled); the module gate and sidebar registry (no route, no `config/modules.php` entry — deliberately, per the story's own D13/OQ-1); and the `<Noun>ValidationRules`/permission-name/`<Model>Policy` sections of [conventions/naming.md](conventions/naming.md) (this story adds no trait, no policy constant and no permission string — its one naming addition is a second `Index`-in-a-subfolder-exception confirmation, `WysiwygEditor` → `wysiwyg-editor.blade.php`, alongside `Media\Gallery`'s).

_Previously: 2026-08-29 — Story 0020 (Shared media gallery modal — frontend): the view half of the Shared Media Gallery ([PRD §2.3](PRD/PRD.md)), and — the property that shapes this pass — **the first story whose central artefact is a contract rather than a screen**. Story 0018 dressed a route that already existed; this one dresses a component that has no route and never will, so what it ships is the four-attribute embed that stories 0021 and 0027 bind to. That is why the docs work is concentrated in two places a UI story normally does not touch: [api/routes.md](api/routes.md), which adds **no table row** and two full subsections, and [architecture/authorization.md](architecture/authorization.md), which gains its **fourth copyable pattern**.

**The pattern is the pass's centre, and it arrived as a real Phase 4 Medium rather than as a retrospective.** [A routeless Livewire component has no per-request authorization backstop](architecture/authorization.md#a-routeless-livewire-component-has-no-per-request-authorization-backstop) is the exact negative of [the module-gate pattern](architecture/authorization.md#the-copyable-module-gate-pattern-and-the-three-alternatives-rejected) at the other end of that page: everything `can:` buys a routed screen has to be paid for by hand here. `mount()`-only gating — correct on all three routed screens, because `can:` **is** on Livewire's `PersistentMiddleware` allow-list and is therefore replayed on every `/livewire/update` round trip — is *not* sufficient for a component with no route, since Livewire replays the **host page's** middleware, which says nothing about `media.*`. Finding **F-1** is the shipped proof: `tiles()`/`toggleSelect()`/`confirmSelection()` kept disclosing the whole library and accepting selections after `media.view` was revoked mid-session. Its real content is the asymmetry the fix required — **a render-path method fails closed, an action method throws** — because a throwing `#[Computed]` propagates out of the child and 500s the *host* page, which is precisely the failure the consumer-side `@can` embed exists to prevent, arriving one layer lower. Three things generalise (enumerate **methods**, not abilities; a `#[Computed]` property **is** a method; a disclosure gate may legitimately ask a **stronger** ability than its neighbours — Phase 5's F-8, recorded on the docblock so nobody "fixes" it into a matching pair), and a ⚠️ notes that the three routed screens are safe under the *current* reading of their own routes rather than permanently. The security-mechanics half is [security/livewire-authorization.md](security/livewire-authorization.md#the-routeless-case-a-component-with-no-route-has-no-per-request-backstop-at-all), whose **opening sentence was also corrected**: it had scoped that whole page to a *"full-page Livewire component"* since task 0004 — true of everything audited through 0018, false as of the first embedded child.

**The sharpest finding of this pass is about the previous one: story 0019's single Phase 6 docs pass reported *two* gaps that were not there, each marked as checked against the tree rather than reasoned about, and both stood for a whole story across four pages.** (1) *"`LogRefusedPrivilegedAttempt` appears nowhere in `app/Livewire/Media/` or `app/Actions/Media/` — **verified by grep** at this story's Phase 6, not assumed"* — `Gallery.php` imports it and calls it twice, and both it and `StoreUploadedImage.php` carry comments naming *that very docs finding* as the reason, so the gap was real when raised, was acted on, and only the write-up was never revisited. (2) *"`media` shipped without its `roles.modules.media` leaf, so the Roles matrix renders a raw key in both locales"*, framed as [conventions/naming.md](conventions/naming.md)'s long-predicted ❌ arriving for real — `lang/en/roles.php` has `'media' => 'Media'` and `lang/es/roles.php` has `'media' => 'Medios'`, both present in story 0019's own tree and untouched since; that one was never true at any point. **Four ⚠️s are corrected in place with what they used to say, never deleted**, because the *rules* they hang on are right and only the claim that this story violated them was wrong: the refusal-logging one is now a ✅ describing the shipped state, the refusal recipe's `mount()`-exclusion list now records `Media\Gallery::mount()` as the deliberate **counter-case** that does log (the other three are excluded because their route's `can:` refuses first — this one has no route to rest on), and the translation-leaf claim is corrected on both the authorization and naming pages. Grew [errors-log.md](errors-log.md) from twenty-six entries to twenty-seven with [the lesson](errors-log.md#one-docs-pass-reported-two-gaps-that-were-not-there-both-marked-verified--2026-08-29), recorded as its own entry rather than folded into [the 2026-08-20 audit-authored-page entry](errors-log.md#a-security-page-documented-the-vulnerable-code-as-current-because-it-was-written-before-its-own-fix--2026-08-20) it resembles, because three of its four causes are new: a pass hunting for gaps grades its findings on how *sharp* they are rather than on whether they hold (both were written up as the pass's most valuable output, and the second was *especially* attractive because the docs had predicted it — a claim that confirms an existing prediction gets less scrutiny, not more); the "verified by grep" framing made a false claim **more** trusted rather than less, the exact inverse of [the 2026-08-24 hedge entry](errors-log.md#a-reviewers-correction-replaced-an-accurate-technical-explanation-with-a-wrong-one-unverified--2026-08-24); and finding 1's fix was **rebased into the earlier commit**, so `git log` order actively argues the false doc is the later, authoritative word — systematically misleading on any rebasing workflow. The habit that catches both, and costs seconds: **re-run the literal command a doc claims to have run, on every "verified by" claim the pass itself wrote.**

**Three further candidates were evaluated and deliberately not logged**, calibrated against this set's own precedents for what has been judged out. The `->wait(5)`/`Playwright::$timeout` finding and the two file-upload impossibilities are browser-testing **environment** rules, which is exactly where the `->waitForEvent('networkidle')` incident was filed despite costing far more — they are now in [testing/frontend/playwright-setup.md](testing/frontend/playwright-setup.md#a-bare-waitn-is-not-a-polling-primitive-and-a-longer-one-can-fail-because-it-is-longer), and they are worth reading before planning any upload coverage: **`attach()` is refused for any file input here** (Playwright's server rejects `localPaths` over this plugin's non-collocated WebSocket connection) **and the plugin's own HTTP driver never parses a multipart body** (`Drivers/LaravelHttpServer.php` builds its Symfony request with a literal `[], // @TODO files...`), so an upload's *completion* is permanently Feature-test territory while everything up to the XHR starting is still coverable. F-1 itself is a mechanical security rule, which this project's precedent keeps on the security page. And the mid-story scope change (D8/D9 deferred, then implemented in full after a Phase 5 review, costing two extra Phase 4 rounds and a third Phase 5 round) was caught by the phase designed to catch it and produced no convention — the same judgement as story 0015b's Phase 3 loop bound.

Also widened: **Routes** (the two new subsections, and the corrected 0019 sentence saying the gates live "inside `mount()` and `upload()`" — two of six now), **Base standards** (`UpdateMediaDetails` as `app/Actions/Media/`'s third class and `MediaPolicy::update()`'s first caller; the new `app/Livewire/Dev/` folder and the four rules that separate scaffolding from surface; and the `Media` `#[Fillable]` ⚠️ turned from a prediction into a record — story 0020 is where a `media` row first gets updated, and it is the case that shows the guard **working**, since the write goes through `$media->update([...])` with a literal two-key array), **Code style** (the `app()` carve-out restated as a rule instead of as its one instance: **a method whose parameter list is fixed by something other than this class** may use it — a `#[Computed]` property and now a Livewire lifecycle hook, which is invoked with fixed parameters rather than through the container), **Testing** and the **Security** entry.

**Verified as unchanged rather than assumed:** [database/schema.md](database/schema.md) and [database/migrations.md](database/migrations.md) — **no migration, no column, no table, no model** (`app/Models/Media.php` is untouched; the `media` table is story 0019's and this story only reads and updates two of its columns), so neither page needed a pass, matching how tasks 0017 and 0018 were handled; [conventions/naming.md](conventions/naming.md)'s rules themselves (`UpdateMediaDetails` is the imperative-verb-phrase action rule applied with nothing to decide, and `MediaGalleryHarness` → `livewire/dev/media-gallery-harness.blade.php` is the ordinary mirror rule — a second confirmation that the `Index`-in-a-subfolder exception keys on the **class name**, not on living in a subfolder); [architecture/overview.md](architecture/overview.md) (no new layer — a routeless component was already recorded as a *shorter* path through the request lifecycle, and this story adds no step to it); the permission catalog at **42** and the policy count at **four**; the module gate and the sidebar registry (story 0020 adds no route and no registry entry, deliberately — D12); the refusal-logging recipe's five steps, which absorbed a fourth domain with no edit to the log line; and the **four** authorization layers in [testing/README.md](testing/README.md), since F-1 is a new rule about layer three rather than a fifth layer._

_Previously: 2026-08-27 — Story 0019 (Media Library: upload, `.webp`/`.avif` conversions and search — backend): the backend half of the Shared Media Gallery ([PRD §2.3](PRD/PRD.md)), and — the property that shapes this pass — **the first story since task 0002 to change the permission catalog itself**. Six months of arithmetic went stale at once: 38 permissions → **42**, nine modules × four → **ten** × four, `Administrator` 37 of 38 → **41 of 42**. That number is prose in eight documents, and the story's own "Files to modify" list named six of them; a whole-tree `grep -rn "\b38\b\|\b37\b" docs/` at this pass found **three more sites the list missed** — five separate counts inside [security/authorization-patterns.md](security/authorization-patterns.md) rather than the one it named, a rendered-checkbox count in [security/README.md](security/README.md), and three passing mentions inside [architecture/authorization.md](architecture/authorization.md) itself, in sections about the Super Admin bootstrap, the `roles.name` collation guard and task 0010's F2. This is the [stale-arithmetic failure mode](errors-log.md#a-docs-this-app-has-no-x-yet-claim-outlived-the-x-by-two-tasks--2026-08-13) at its most mechanical, and the reason the story file's own instruction was to treat its docs list as a floor.

Rewrote the **Authorization** entry around [The `media` module, and what a catalog amendment costs](architecture/authorization.md#the-media-module-and-what-a-catalog-amendment-costs) — a section that exists because *"a story that needs a new permission adds it to `MODULES`"* is a one-line instruction whose real cost is fifteen hardcoded test assertions plus this doc set — and around [`MediaPolicy`](architecture/authorization.md#mediapolicy--the-fourth-policy-and-the-first-behind-no-route-at-all), the **fourth** policy, the second with no target-dependent branch, and the first defending a model with **no route in front of it**. That last property is what makes this story architecturally new rather than additive: PRD §2.3 makes the gallery a *modal Products and Blog embed*, so there is no `GET /media`, no `can:` middleware and no `config/modules.php` entry, and the in-component `Gate::authorize()` calls stop being the second of two layers and become the whole perimeter. [api/routes.md](api/routes.md) — which ships **no** new row this story — gained one ⚠️ for exactly that reason: a reader auditing "what is reachable and what protects it" from that page alone would now miss a gated surface.

**Two findings came out of reading rather than from the change→doc mapping, and both are recorded as gaps rather than smoothed over.** [**Correction, 2026-08-29 (story 0020): neither gap existed.** Both claims below are false — the `roles.modules.media` leaf is present in both locales and always was, and `LogRefusedPrivilegedAttempt` is imported and called in `Gallery.php` and `StoreUploadedImage.php`. The paragraph is left standing as the record of what this pass asserted; [errors-log.md](errors-log.md#one-docs-pass-reported-two-gaps-that-were-not-there-both-marked-verified--2026-08-29) records how one pass produced two false "verified" findings, and every page that carried them is corrected.] First, the catalog amendment shipped **without** its `roles.modules.media` translation leaf, so the Roles permission matrix renders the raw key `roles.modules.media` as a row label in both locales — with a fully green suite, because the seeder tests assert names and counts and never rendered copy. That is precisely the ❌ [conventions/naming.md](conventions/naming.md) has predicted since task 0011, arriving for real; it is now a ⚠️ on both that page and the authorization one, with the rule that a module slug and its leaf move together. Second, `App\Livewire\Media\Gallery` **logs no refusals at all** — `LogRefusedPrivilegedAttempt` appears nowhere under `app/{Livewire,Actions}/Media/`, verified by grep — on the one screen where [the refusal-logging recipe](architecture/authorization.md#recording-a-refusal--what-every-gate-owes-the-audit-trail)'s own argument is strongest, since with no route in front of it a refused fiftieth upload probe and a successful upload are the same `POST` in an access log. Neither adopted nor rejected by any phase; recorded so story 0020 meets a decision rather than a silence.

**Closed a deferral this doc set has carried since task 0016.** [decisions/0001-uuid-primary-keys.md](decisions/0001-uuid-primary-keys.md) gained [Amendment 1](decisions/0001-uuid-primary-keys.md#amendment-1-2026-08-27--the-scope-is-the-policy-not-the-list-of-seven): `media` is the **second** UUIDv7 table the ADR's seven-entity list does not name, and leaving it open a second time is the same stale-claim mode as everything above. The amendment states the **policy** — every new business entity is UUIDv7, one named `bigint` exception — rather than extending an enumeration a third story would extend again, and records honestly that the ADR's stated enumeration-safety rationale fits `media` and does *not* fit `sales_regions`.

Also widened **Schema** ([`media`](database/schema.md#media): verified column order from `php artisan db:table media` rather than the migration file, the three-path-columns decision, the three real indexes and four omissions, why `FULLTEXT` would *break* partial matching rather than merely cost a write, and the two ⚠️s — `nullOnDelete()` will essentially never fire against a soft-deleted `users`, and this is the first table whose contents are only half in the database, which is why `composer.json`'s `setup` script gained `storage:link`), **Base standards** (`app/Actions/Media/` as the fourth area folder; `config/livewire.php` published for a *security* reason rather than a preference; `Media` as the `#[Fillable]`-omission guard's most lopsided case at three fillable against seven omitted), **Naming**, **Testing** and the **Security** entry — whose promise to summarise [image-upload-processing.md](security/image-upload-processing.md) "at the end of this entry" was written during Phase 4 and never delivered; the summary now exists.

[architecture/overview.md](architecture/overview.md) needed a pass of its own for the same arithmetic reason — four enumerations falsified (the permission count, the three-policies list, `app/Models/` holding three classes, and the action layer being on its third area) plus one *added* row, **Uploaded files**, because this is the first story whose runtime state is partly not in the database. Its **request-lifecycle diagram is deliberately untouched**: a routeless component is a *shorter* path through it rather than a new one, entering at the Livewire node instead of at Routes.

**Verified as accurate against `HEAD` rather than rewritten:** [security/image-upload-processing.md](security/image-upload-processing.md) itself (audit-authored as ❌/✅ pairs describing the shipped state from the outset — the first time this project's [audit-authored-page rule](errors-log.md#a-security-page-documented-the-vulnerable-code-as-current-because-it-was-written-before-its-own-fix--2026-08-20) was applied *prospectively*) and its link from [security/README.md](security/README.md), where the "eleventh page" count matches `ls docs/security/*.md` — twelve files, eleven pages besides the index; [conventions/code-style.md](conventions/code-style.md) (`StoreUploadedImage` constructor-injects `GenerateImageConversions` for the reason [the documented exception](conventions/code-style.md#exception-an-actions-own-dependency-is-constructor-injected-when-the-method-signature-is-a-public-contract) already gives — `__invoke()`'s parameter list is a public contract — which is that rule applied, not a third case); the **four** authorization layers in [testing/README.md](testing/README.md) (a fourth *policy* is not a fifth layer); and [errors-log.md](errors-log.md), where the three surviving "38" mentions are dated historical records of what the catalog was at the time and are correct as written.

**No new `errors-log.md` entry from this pass.** The story's two Phase 4 blocking findings produced mechanical security rules, which this project's precedent keeps on the security page rather than duplicating here; its re-audit finding N-6 (the task file's own Phase 4 record naming three of six findings) is [the record-of-positives entry](errors-log.md#a-verification-record-that-lists-two-of-three-quality-gates-is-a-record-of-two-gates--2026-08-26) recurring with its prescribed fix already applied; and the two gaps this pass found are documented where they will be met rather than as lessons, since neither has yet cost anything._

_Previously: 2026-08-26 — Task 0018 (Sales Regions & Taxes screen — UI): the view half of the story 0017 shipped one day earlier, and — the property that shapes this pass — **the first story whose docs work is dominated by correcting statements this doc set made about itself twenty-four hours ago**. Nothing here was wrong when it was written; three claims were simply written against a tree 0018 then changed, which is this set's [bare-negative-claim](errors-log.md#a-docs-this-app-has-no-x-yet-claim-outlived-the-x-by-two-tasks--2026-08-13) failure mode running at its shortest fuse yet. Rewrote the **Routes** entry, where two of them lived: `sales-regions.index`'s view was described as *"a deliberate **placeholder**"* and the route as *"the half of a module gate that currently has **no** `config/modules.php` entry"* — both false as of this story, both corrected in place with what they used to say recorded, and the subsection's bullet list grown from nine to twenty, split explicitly into 0017's backend contract and 0018's screen. The third was in **Authorization**, whose Current-state bullet ended *"until story 0018 adds one"*; it now records that 0018 completes the set — 0017 exercised the module gate and the refusal-logging recipe, 0018 exercises the third pattern — and the sidebar-registry section gains the ✅ that matters most to a later epic: the cost really was **two array literals and two lang leaves with the reading component untouched**, verified with `git diff --stat` rather than asserted, plus the three properties the first three registry entries could not demonstrate (a multi-word key is snake_case and does three jobs at once; adding a *group* is data too; both generic drift guards picked the entry up for free, which the story's own plan assumed the opposite of). Widened **Naming** (the registry-mirroring rule's multi-word clause has named `items.sales_regions` by name since task 0013 and stopped being hypothetical here; plus the new rule that **a `label()` method on an enum is not automatic** — 0016 deferred `SalesRegionKind::label()` to the first story that renders `kind`, and that story declined it), **Base standards** (the app-owned-config rule's first ✅, and a corrected `tests/Browser/` directory line), and **Testing** (the suite's third browser file, this repo's **waiting rules**, the icon-only-selector ⚠️, and the folder-structure lesson aimed one step earlier — a story file that names a test path is making a convention decision, so the path belongs in the Phase 2 review). **The sharpest finding of the pass was in a page this story had already written.** [errors-log.md](errors-log.md)'s new entry (the `@directive()` compilation trap) was carried forward under this set's own [audit-authored-page rule](errors-log.md#a-security-page-documented-the-vulnerable-code-as-current-because-it-was-written-before-its-own-fix--2026-08-20), and re-verifying it disproved its own root cause: executed through `Blade::render()` **and** through the real file-view compilation path, the exact shipped two-`@js()` `<flux:switch>` expression compiles fine, while an anonymous `<x-…>` component leaves `@js()` uncompiled at **any** count. The count was never the discriminator. The tell was in the entry itself — it needed a carve-out for this repo's own two-`@js()` `x-show`, and that carve-out was a **hedge**, which [the 2026-08-24 entry](errors-log.md#a-reviewers-correction-replaced-an-accurate-technical-explanation-with-a-wrong-one-unverified--2026-08-24) says to read as "nobody ran this". Added a dated correction with the executed results, the one half that *is* now verified, and a deliberate refusal to guess at the real mechanism — the broken state was never committed, and replacing one confident wrong explanation with another would repeat the mistake being corrected. Also fixed the errors-log's own footer and this file's count, both of which the mid-story commit left at twenty-five. **Verified as accurate against `HEAD` rather than rewritten:** [security/livewire-authorization.md](security/livewire-authorization.md)'s `$toggle` confirmed-safe subsection and its [security/README.md](security/README.md) index entry (both written during this story's Phase 4 — every vendor code quote, the `#[Locked]` claim and the shipped `wire:click="$toggle('active')"` binding re-checked, and correct); [database/schema.md](database/schema.md) and [database/migrations.md](database/migrations.md) (**no** migration, column, model or table — this story writes markup, config data and lang keys); [conventions/code-style.md](conventions/code-style.md) (no action, no injection site, no PHPDoc shape — the story's one PHP line is a `resetValidation()` call inside 0017's `closeModal()`); [architecture/overview.md](architecture/overview.md) (no new layer — a third module screen walks the path already drawn, and `config/modules.php`'s registry row already exists); the **four**-authorization-layer count in [testing/README.md](testing/README.md); and the registry's two ⚠️ hazards on the authorization page, which are still unexercised — the new entry holds a single ability like every other, and its `group` key resolves._

_Previously: 2026-08-26 — Task 0017 (Sales Region tax configuration — backend): the first new gated route since task 0010, and — the property that shapes this whole pass — **the first story that mostly *confirms* this doc set rather than correcting it**. Its route, its policy, its per-action authorization and its refusal logging were all built by following patterns `docs/` already described prospectively, so the honest sync is largely arithmetic (two gated routes → three, two policies → three, five domain actions → eight, two admin screens → three) plus one genuinely new concept. Rewrote the **Authorization** entry for [`SalesRegionPolicy`](architecture/authorization.md#salesregionpolicy--the-third-policy-and-the-first-with-no-target-branch) — the smallest policy in the app and the most instructive read *against* the other two, since almost everything it does not do is a recorded decision — and for the new boundary section [A domain invariant is not an authorization rule](architecture/authorization.md#a-domain-invariant-is-not-an-authorization-rule-and-does-not-live-here), which is what this story adds that no existing section covered: a rule about the shape of the **data** rather than about who may act, which therefore can never be a policy method (a Super Admin can leave the catalog defaultless like anyone else), runs strictly after authorization, must re-read its subject under lock, and is still logged like a refusal. Rewrote the **Routes** entry for `sales-regions.index`, scoped honestly to a backend-only story: the view is a **placeholder**, the sidebar registry entry does not exist yet (0018's), and `ModuleRouteAccessTest.php` was not extended — recorded as a gap, not a decision. Widened **Base standards** (the action-owns-the-rule convention's first cost-free application, plus the ⚠️ that "authorize before the first write" and "re-read what you authorize against" pull in opposite directions once an action locks its own rows), **Naming** (a third `Index`-in-a-subfolder row and the kebab-casing it finally demonstrates; the `<noun>Rules()`-names-the-field rule; the mid-story `attributes`-block camelCase exception, **verified rather than duplicated**), **Overview** and **Security**. Grew the **Errors log** from twenty-three entries to twenty-four with the verification records that named two of three quality gates — deliberately its own entry rather than folded into the 2026-08-20 scoped-gates one, because the two failure modes are opposites (a gate that ran and saw almost nothing, versus a gate that never ran). **Four things were corrected that this story's own Definition of Done does not name, all found by re-reading and counting rather than by the change→doc mapping**, and three of them are under-counts, which is this set's recurring [bare-negative-claim](errors-log.md#a-docs-this-app-has-no-x-yet-claim-outlived-the-x-by-two-tasks--2026-08-13) failure mode arriving as arithmetic: [architecture/overview.md](architecture/overview.md) still opened *"The domain layer beyond `App\Models\User` does not exist yet"* — falsified by **task 0016**, whose own pass fixed the identical sentence in `database/schema.md` and missed this copy — and carried four diagram node labels that enumerated one item of three or four; [testing/backend/feature-integration-tests.md](testing/backend/feature-integration-tests.md) named one module gate and one policy in its four-layer list; [security/README.md](security/README.md)'s index entry stopped at Phase 4 round 2 while the page gained two Phase 5 corrections the next day; and that same file called `model-instance-trust.md` "the eleventh page" when `ls docs/security/*.md` returns ten. **Verified as accurate against `HEAD` rather than rewritten:** [security/model-instance-trust.md](security/model-instance-trust.md) itself (every ❌/✅ code block re-checked against the three shipped actions, including the `whereKeyNot()` → `$rows->reject(...)` supersession and the claim that nothing in this app reads `SetSalesRegionActive`'s return value); [database/schema.md](database/schema.md) and [database/migrations.md](database/migrations.md) (**no** migration, column or table — `sales_regions` is task 0016's, and this story writes only application code on top of it, so its `is_default` ⚠️ about no database-level single-default constraint is still true and its deferred backstop is still deferred); [conventions/code-style.md](conventions/code-style.md) (the three actions constructor-inject their dependencies and the component method-injects — the existing rule and its documented exception applied unchanged, not a third case); the **four**-authorization-layer count in [testing/README.md](testing/README.md) and [testing/backend/feature-integration-tests.md](testing/backend/feature-integration-tests.md) (a domain invariant is not a fifth access-control layer); and [conventions/base-standards.md](conventions/base-standards.md)'s app-owned-config rule (`config/modules.php` is untouched by this story)._

_Previously: 2026-08-24 — Task 0015b (log refused privileged attempts — the F-C split from story 0015). Like its two siblings it ships **no route, ability, policy method, column, migration, schema or UI change**, and — the property that governs this whole pass — **no refusal's class, status code, message, validation field or timing changed either**: logging here is observation, never handling, so almost nothing this doc set already said about *behaviour* became false. What it adds is a property none of those docs covered. Rewrote the **Authorization** entry for the page's third copyable pattern, [Recording a refusal](architecture/authorization.md#recording-a-refusal--what-every-gate-owes-the-audit-trail), which sits beside the module gate and the sidebar registry as the next thing a later epic's admin screen inherits rather than re-invents; its ⚠️ closes the story's Phase 4 finding **F-3** — a refusal on these screens produces one of **two** message strings, since 0015a's `'Step-up password confirmation required'` is deliberately left unfolded, so a level filter catches both while a message filter needs both. Widened the **Security** entry ([livewire-authorization.md](security/livewire-authorization.md) gains the observability half of its own two-year-old "gate every method that mutates *or discloses*" rule, including why `mount()` is the one deliberate exception — that page's allow-list table read in the **opposite** direction from the `password.confirm` row directly above it), **Base standards** (`app/Actions/Auth/`'s second inhabitant, the case that *confirms* the cross-cutting-concern rule rather than merely following it), **Code style** (the constructor-injection exception's fallout: an action is resolved from the container, never `new`-ed, proven by three actions gaining their first constructor dependency and breaking ten `new RequestEmailChange` call sites) and **Overview** (one corrected layer row). Grew the **Errors log** from twenty-two entries to twenty-three with the screen-shaped scope exclusion that could not bind a shared class; **the story's Phase 3 off-by-one loop bound was evaluated and deliberately not logged** — caught inside the phase that produced it, absent from the task file's own record, and yielding no lasting convention. **Two files this story's Definition of Done does not name were corrected anyway, found by grepping the tree rather than by the change→doc mapping** — [architecture/overview.md](architecture/overview.md) and [conventions/base-standards.md](conventions/base-standards.md), both of which described `app/Actions/Auth/` as holding one class, which is the same [bare-negative-claim](errors-log.md#a-docs-this-app-has-no-x-yet-claim-outlived-the-x-by-two-tasks--2026-08-13) failure mode arriving as an under-count that 0015a's own pass had to fix five times. **Verified as accurate against `HEAD` rather than rewritten:** [api/routes.md](api/routes.md) (no route's URI, name, middleware or handler changed; the components' added parameters are container-resolved and change nothing a `wire:click` passes, and no `data-test` hook or DOM contract moved), [database/schema.md](database/schema.md) and [database/migrations.md](database/migrations.md) (no column, model or migration in the diff), [conventions/naming.md](conventions/naming.md) (`LogRefusedPrivilegedAttempt` is an imperative verb phrase with no `Action`/`Service` suffix and the non-`Gate` reason strings are snake_case — both existing rules, applied, neither newly ruled on) and [testing/README.md](testing/README.md) (the story adds four test files to two existing folders and no new authorization layer — there are still four)._

_Previously: 2026-08-24 — Task 0015a (step-up authentication for privileged Users actions — the F13 split from story 0015). Unlike its sibling, this story **adds** rather than corrects: a new `app/Actions/Auth/` folder, a new domain exception rendering **423**, an app-added rate limiter on a vendor route, three modal notices — but still **no route, ability, policy method, column, migration or schema change**, and `routes/users.php` / `App\Policies\UserPolicy` were verified unchanged rather than assumed, since that is an explicit acceptance criterion. Rewrote the **Authorization** entry for the page's first structural addition since task 0004: a **third authorization layer**, distinct from route middleware and policies because it answers a question about the *person* rather than the *account*. Widened the **Authentication** entry (Fortify's password-confirmation flow now has a consumer outside the auth screens, and `POST /user/confirm-password` is rate-limited **by this app** because Fortify's routes consult no limiter config key for it), the **Routes** entry (`users.index`'s bullet list from seven to nine, the narrowed `security.edit` sentence, and the new Fortify-route paragraph), **Overview** (the request-lifecycle diagram gained a `Step-up guard` node — the first lifecycle change since task 0004 — plus two "Where things live" rows), **Base standards** (`app/Actions/Auth/` and the cross-cutting-concern-vs-module-area rule; `PasswordConfirmationRequiredException` → 423; the two new test folders; and the action-owns-the-rule convention's *weaker* second case), **Naming** (the boolean rule binds `#[Computed]` methods, and a predicate must be unambiguous read out of its class), **Security** (the ninth page, [step-up-authentication.md](security/step-up-authentication.md)) and **Testing** (three→four authorization layers). **Four files this story's Definition of Done does not name were updated anyway, found by grepping the tree rather than by the change→doc mapping** — every one of them an enumeration that had silently become an under-count, which is the [bare-negative-claim](errors-log.md#a-docs-this-app-has-no-x-yet-claim-outlived-the-x-by-two-tasks--2026-08-13) failure mode arriving as arithmetic instead of as a denial: [architecture/authentication.md](architecture/authentication.md) and [architecture/overview.md](architecture/overview.md) as above, plus [testing/backend/feature-integration-tests.md](testing/backend/feature-integration-tests.md), [testing/qa/what-not-to-test.md](testing/qa/what-not-to-test.md) and [testing/qa/risk-based-testing.md](testing/qa/risk-based-testing.md) (all three said "three layers" and none of their rules reached a layer that is neither an ability nor a policy), and [testing/frontend/gherkin-guidelines.md](testing/frontend/gherkin-guidelines.md), whose domain glossary had one word for two now-different things. **Verified as accurate against `HEAD` rather than rewritten:** [security/step-up-authentication.md](security/step-up-authentication.md) and its [security/README.md](security/README.md) index entry, both already corrected during this story's own Phase 5 pass (every code quote, both logout paths' `Session::invalidate()` calls and all five cited test files re-checked); and [database/schema.md](database/schema.md) and [database/migrations.md](database/migrations.md), untouched because the diff contains no column, model or migration. **No `errors-log.md` entry from this pass** — the story's own entry (a review "correction" that replaced accurate prose with wrong prose) was already added during Phase 5; the index entry above is updated from twenty-one entries to twenty-two to match._

_Previously: 2026-08-24 — Task 0015 (harden the Users CRUD backend's security posture — the consolidated follow-up to story 0004's ten deferred audit findings). It ships **no route, ability, policy method, column, migration or schema change**, so this pass is almost entirely **corrections to statements that stopped being true**, not additions. Rewrote the **Routes** entry and the **Authorization** entry around the same falsified pair: the per-row `canEdit` hint no longer reads `Gate::allows('update', $user)` but mirrors `openEditModal()`'s new `updateSensitiveAttributes` gate plus a self-row identity check, which flips an Administrator-holding **other** target's row from enabled to disabled and removes that actor's ability to rename such a user at all — a deliberate narrowing, recorded as one. The `Gate::allows()`-is-a-UI-hint section's single accepted drift became **two**, both for one structural reason (a `Gate::allows()` hint is blind to any rule that deliberately lives outside `Gate`), the second being the self-delete guard (F11), which is why both "the delete path has no equivalent guard" statements — including a ⚠️ claiming a Super Admin can delete "their own account" — are now narrowed rather than left. Widened the **Security** entry twice: `livewire-authorization.md`'s "gate every method that mutates *or discloses*" rule, stated since task 0004 while the screen it was written about gated **none** of its three openers, finally has its shipped example (with why the disclosure check is the *stronger* ability, and why the component performs no tier lookup to get there); and `authorization-patterns.md` gains a new rule — a rate limit keyed on the **target** alone becomes an attack on the target once a cross-user caller exists — plus a confirmed-safe addendum to the relation-reload rule, whose `load()`-not-`loadMissing()` clause is now load-bearing against this repo's own code. Grew the **Errors log** from twenty entries to twenty-one. **Three files this story's Definition of Done does not name were corrected anyway, found by grepping and re-reading rather than by the change→doc mapping:** [architecture/authentication.md](architecture/authentication.md) carried three false statements (`UserInvitation` marked `ShouldQueue`, and the pending-email throttle described as one target-keyed limiter, in both its prose and its sequence diagram); [security/blade-livewire-output-encoding.md](security/blade-livewire-output-encoding.md) described `$users` as **deliberately left unlocked**, an accepted residual whose stated cost was exactly the two tests finding F4 rewrote — now recorded as closed, with a note that the same bullet's "every mutating **and disclosing** method re-authorizes" clause was *aspirational* when written, since the three openers had no authorization until this story; and [conventions/base-standards.md](conventions/base-standards.md) needed the **converse** of its action-owns-the-rule convention stated, since this story adds a `Gate::authorize()` to a component and a mechanical reading would try to relocate it. **Verified as unchanged rather than assumed:** [database/schema.md](database/schema.md) and [database/migrations.md](database/migrations.md) (no column, model or migration in the diff), and the rest of **Base standards** (no new folder — the five new test files land in the existing `tests/Feature/Users/`)._

_Previously: 2026-08-22 — Task 0013 (module/sidebar access gating — UI): the story that closes the gap tasks 0006 and 0011 both named by number. It ships **no route, ability, column, migration or schema change** — it adds this repo's first **declarative permission-driven UI registry** (`config/modules.php`), one anonymous Blade component reading it, and `lang/{en,es}/navigation.php`. Rewrote the **Authorization** entry for the new **second half of a module gate** section (the registry's copyable entry shape, its five rules, and the two ⚠️ hazards a later epic meets first), and narrowed that entry's pre-existing `.view`-shaped-gate ⚠️, which the sidebar now makes silent in the navigation too. Rewrote the **Routes** entry: both gated subsections' sidebar bullets asserted "no permission gating" and "story 0013 … is not yet built", false as of this story, and the preamble now records that a gated route is only half the job. Widened the **Security** entry — its registry rule is **closed**, with both ✅ blocks now the shipped guard tests instead of recommendations — plus **Base standards** (when an app-owned config file is the right shape; no closures, because `config:cache` uses `var_export()`), **Naming** (a registry's lang file mirrors the registry's keys) and **Overview** (the new layer row). **No `errors-log.md` entry**, deliberately: the story's two Phase 5 informational findings are forward-looking hazards rather than mistakes made, and they are documented as ⚠️ where a later epic will meet them; its Phase 4 findings produced mechanical security rules, which this project's precedent keeps on the security page rather than duplicating here. Nothing in **Schema**, **Migrations** or **Testing** changed meaning — verified against the real diff rather than assumed. Two stale rows in `architecture/overview.md`'s layer table were corrected in the same pass, found by re-reading rather than by the change→doc mapping: **Routes** omitted `routes/roles.php` (task 0010) and **Domain actions** omitted `app/Actions/Roles/**` (task 0009)._

_Previously: 2026-08-21 — Task 0012 (module/sidebar access gating — backend): a story that ships **no new route, ability, column, migration or contract** — both module routes were already correctly and independently gated by closed stories 0004/0040 and 0010 — so this pass is almost entirely about a rule that existed only in a task file. Widened the **Authorization** entry for `architecture/authorization.md`'s new **copyable module-gate pattern** section (Phase 5 finding F1: acceptance criterion 6 required the pattern and its rejected alternatives to be documented, and a repo-wide grep for "route sugar" / "group-level" returned zero hits — the group-level-gate and `->can()`-sugar rejections lived nowhere a later epic's developer would look), including the ⚠️ for F2's dead `.view` configuration. Widened the **Routes** entry, where the pattern is now *pointed to* rather than duplicated, and the **Security** entry for the confirmed-safe 403 rule Phase 4 wrote plus the cache-flush ⚠️ added in this pass. Grew the **Errors log** from nineteen entries to twenty with the transaction wrapper that relocated a flush nobody had written. Nothing in **Schema**, **Migrations**, **Base standards**, **Naming** or **Testing** changed meaning: verified rather than assumed, by re-reading each against this story's real diff (one production file, one test file)._

_Previously: 2026-08-21 — Task 0011 (Roles & permissions management — UI): the Blade view sibling story 0010 closed without. Rewrote the **Routes** entry, which repeated 0010's now-false "`GET /roles` does not render yet" claim, around what the shipped view actually renders. Widened the **Authorization** entry for the `Gate::allows()` hint's second call site and for the two forward-looking ⚠️s this story turned into shipped behaviour, the **Security** entry for `authorization-patterns.md`'s new DOM-omission section (written during this story's own Phase 4 audit and corrected at Phase 5 when its code quote went stale — the audit-authored-page lesson applied in the same story that names it), and the **Naming** entry's home file for the composed-label / snake_case-leaf convention and the `trans_choice()` rule binding a Blade template. Grew the **Errors log** from eighteen entries to nineteen with the count assertion that counted a wrapper element. No architecture, schema or route *contract* changed: this story adds no route, no column, no migration and no ability._

_Previously: 2026-08-21 — Task 0010 **Phase 7 closure** (link integrity, no code change): widened the **Workflow** entry now that [workflow.md](workflow.md#link-integrity-check-on-every-stage-move)'s stage-move rule covers **both** link directions. It previously described only the moved file's own outbound links breaking on a depth change; closing this story surfaced the mirror failure — ten stale **inbound** links across four task files (`0011`, `0012`, `0035`, `done/0009`) still pointing at 0010's original `ai-spec/tasks/` path, broken since its Phase 3 move and invisible for three phases because the citing files are untouched by the move. The rule now mandates a repository-wide grep for the moved file's basename plus per-citer path recomputation, and records that `in-progress/` → `done/` is a same-depth move, so the outbound half is a no-op at Phase 7 while the inbound half is not. No architecture, schema, route or security entry moved in this pass._

_Previously: 2026-08-20 — Task 0010 (Roles & permissions management — backend). Rewrote the **Authorization** entry around two new sections in `architecture/authorization.md` — **The Administrator tier's immutability** (the tier's row is now name-locked and undeletable while its permission set stays editable) and **The second grant meta-rule: you cannot grant what you do not hold** (`EnforceGrantorPermissionScope`) — and marked 0009's transformer-not-writer limitation resolved rather than deferred. Rewrote the **Routes** entry for `roles.index`, the second permission-gated route. Widened the **Schema** entry (the `roles` collation guard now lives on the model as `firstOrCreateAdministratorRole()`, throwing `ImmutableRoleException` rather than the `RuntimeException` the doc claimed, plus the holder-count 409 guard), **Base standards** (the unscoped-gate rule), **Naming** (`trans_choice()` plurals, `RoleValidationRules`, the second `Index`-in-a-subfolder row), and **Security** (one new `authorization-patterns.md` rule: an identity derived from a mutable column must be locked once code can mutate it). Grew the **Errors log** from seventeen entries to eighteen with the two scoped-by-default quality gates that both silently passed. Five stale claims this story falsified were corrected in the same pass rather than only appended to — `RolePolicy` "has three abilities" and "no call site in `app/`", the seeder's `firstOrCreate()` quote in two places, the `Role::boot()` quote, `selectable()`'s "first and today only caller", and the forward-looking `Gate::before` warning aimed at this very story, now recorded as verified not to bite (`viewAny`/`create` are asked class-level, and the deferral keys on `$target instanceof Role`)._

_Previously: 2026-08-20 — Task 0040 (move `users.index` into its own `routes/users.php`): a pure relocation, so no index entry changed meaning — but six files named `routes/web.php` as where the app's one gated route lives, and all six were corrected: `api/routes.md` (scope sentence, "it lives in" sentence, code quote), `architecture/authorization.md` (the ✅ gate quote, the inline-comment sentence, the **Where it lives** row), `security/livewire-authorization.md` (the `can:`-gated route quote), `conventions/base-standards.md` (the `routes/` listing, plus the one-file-per-area convention `web.php` now follows), `architecture/overview.md` (flowchart node, entry-points bullet, "Where things live" row) and `testing/qa/risk-based-testing.md` (question 4's citation). The three surviving `routes/web.php` mentions elsewhere are about the dashboard and login routes and are correct as they stand. Grew the Errors log from sixteen entries to seventeen with the `verified`-middleware test that could carry no signal._

_Previously: 2026-08-20 — Task 0009 (Administrator-level permission grant): widened the Authorization entry for `RolePolicy`'s third ability and Administrator-level branch and for the new **Who may grant a permission** section, and marked the roles-CRUD partial-hydration residual **closed** rather than outstanding. Expanded the Base standards entry (`app/Actions/Roles/`), the Naming entry (permission names as class constants), the Security entry (two new `authorization-patterns.md` rules, plus that page's residual closed), and grew the Errors log from fourteen entries to fifteen with the guard that took the state it guarded as a parameter. Two stale claims this story falsified were rewritten in `architecture/authorization.md` in the same pass — its `Gate::before` code quote and its `Role::superAdminName()` quote — found by re-reading the page against the diff rather than by the change→doc mapping alone. No route doc change: this story adds no route (that is story 0010), and no schema doc change: it adds no column, table or migration._

_Previously: 2026-08-20 — Task 0016 (Sales Region catalog schema and seeder): the first Epic 2 domain table, and the repo's first greenfield UUID `create_*` migration. Rewrote the Schema entry around the new [`sales_regions`](database/schema.md#sales_regions) section (the one-level-deep `parent_id` tree, the seeder-owned vs. administrator-configurable column split, the verified index list) and the Migrations entry now that its UUID section cites a real file instead of a target snippet, including the new "an FK column does not also get an explicit `index()`" rule. Widened the Base standards entry for the new `database/data/` bundled-fixture folder (the ISO 3166-1 country list `SalesRegionSeeder` reads, and `ProductionSeeder`, which composes the required catalogs so a production runbook names one class forever) and for `SalesRegion` as the reference case for the `#[Fillable]`-omission guard. Three stale claims this story invalidated were corrected in the same pass — `database/schema.md`'s "no domain model exists beyond `User`" and its ADR 0001-only UUID entity list (`sales_regions` is an eighth, under a policy whose ADR amendment is deferred), and `conventions/base-standards.md`'s matching preamble. The Phase 4 audit added no new security page — it expanded [`security/seeder-safety.md`](security/seeder-safety.md), whose "fail loudly" section this pass **corrected**: it still quoted the pre-fix `$spain?->id` code as current, and is now an explicit ❌/✅ pair against the shipped `throw_if(...)` guards. Grew the Errors log from fourteen entries to fifteen with that doc-drift lesson._

_Previously: 2026-08-19 — Task 0008a (centralize Administrator-level role identification): rewrote the Authorization entry around the new **The Administrator tier's identity** section, and widened the Base standards, Schema, Naming, Routes, Security and Errors log entries. Three statements the story's own Definition of Done named as falsified were corrected in `architecture/authorization.md` (`RoleName` "is never an identity check", now stated per case; the partial-hydration ⚠️, narrowed rather than deleted; and the `users.index` re-authorization description in `api/routes.md`), plus three more the Phase 5 review added (the "consistent with the mutating path, which grants them too" claim, its stale `hasRole('Super Admin', 'web')` quote, and `api/routes.md`'s "cannot drift"). Two further stale claims this story invalidated were found by grepping rather than by the change→doc mapping: `security/livewire-authorization.md`'s "`CreateUser` and `UpdateUser` carry **no** authorization of their own" (the whole section rewritten around the gap now being closed) and `security/authorization-patterns.md`'s deferred role-shaped-predicate residual, which is a confirmed product decision rather than an open item. The three Phase 4 rounds added no new security page — they expanded `security/authorization-patterns.md` with two rules (direct throw over `Gate` for anything binding a Super Admin actor; reload a relation above the check that reads it). Grew the Errors log from thirteen entries to fourteen with the process lesson: two of three audit rounds found the flaw in the previous round's fix._

_Previously: 2026-08-18 — Task 0008 (Super Admin role invariants): rewrote the Authorization entry around the new **The Super Admin role's invariants** section, and rewrote that page's now-stale `Gate::before` code quote for its `Role::superAdminName()` call and the target-is-the-Super-Admin-role deferral. Expanded the Base standards entry for `app/Exceptions/` (folded into the existing "stock Laravel locations" sentence) and the Schema entry for the `roles` table's new model with no schema change. Two stale bare-negative claims this story invalidated were corrected in the same pass, found by grepping the tree rather than by the change→doc mapping: `architecture/overview.md`'s and `database/schema.md`'s "`app/Models/` contains only `User.php`". Grew the Errors log from twelve entries to thirteen with the vacuous `arch()` rule. This story's three Phase 4 audits added no new security page — they expanded `security/authorization-patterns.md`, which `appsec-auditor` wrote itself along with its `security/README.md` entry (verified, links resolve); the only sync needed here was a pointer noting where the shipped double fallback now lives._

_Previously: 2026-08-17 — Task 0007 (non-active status blocks sign-in): rewrote the Authentication entry for the new sign-in block and its three enforcement points, added the authentication-control note to the Schema entry's `users.status` coverage, indexed the eighth security page (`login-status-enforcement.md`, from this task's Phase 4 audit and re-audit), and grew the Errors log entry from eleven to twelve with the `getPrevious()`-not-`getOriginal()` mistake. Three stale claims in `architecture/authentication.md` that this story invalidated were corrected in the same pass — the "once the sign-in block story lands" future tense, the pre-`getPrevious()` `ActivateVerifiedUser` code quote, and the wrong reason given for that listener's `Inactive`-only guard._

_Previously: 2026-08-17 — Added the "Link-integrity check on every stage move" rule to the Workflow entry: a task file's relative links are written for its `new`-stage directory depth and silently break one directory-level shallow after moving to `in-progress/`/`done/`. Found and fixed the real break across six already-`done` task files (`0002`–`0006b`), and grew the Errors log entry from ten to eleven._

_Previously: 2026-08-16 — Task 0006 follow-up (three post-closure changes to the same screen): expanded the Authorization entry for the per-row `Gate::allows()` UI hint that disables a row action the actor may not perform, the Routes entry for what that branch changes in `resources/views/livewire/users.blade.php`, and the Errors log entry from seven to ten — adding the `null`-property/native-`<select>` desync bug and the two Flux/Blaze rendering traps (`tooltip` presence under Blaze, `disabled:cursor-*` under `pointer-events-none`)._

_Previously: 2026-08-16 — Task 0006 (Users list + create/edit modal UI): rewrote the Routes entry and `api/routes.md`'s stale "the view behind it is a placeholder" bullet to describe what `resources/views/livewire/users.blade.php` actually renders (table, status badges, pending-email marker, both modals, empty state, icon-only `data-test` row actions, the ungated sidebar link). Indexed the seventh security page (`blade-livewire-output-encoding.md`, from this task's Phase 4 audit) and the seventh errors-log entry (parallel agents writing the same file), and recorded the new **Parallel Agent File-Ownership Rule** added to `contracts.md`._

_Previously: 2026-08-16 — Task 0006b (wire up the `tests/Browser/` suite): updated the Frontend/browser testing entry — the suite is no longer pending, and CI runs it Chromium-only. Also indexed the sixth security page (`ci-workflow-hardening.md`, from this task's Phase 4 audit), which the index was missing. The change otherwise cascaded into `testing/frontend/playwright-setup.md` (owner), `testing/frontend/README.md`, `testing/philosophy.md`, `testing/backend/database-strategy.md`, `testing/backend/unit-tests.md`, `testing/ci/commands.md`, `testing/ci/pipeline-integration.md` and `conventions/base-standards.md`._

_Previously: 2026-08-14 — Task 0005 (soft-delete users + administrator-level protection guard): expanded the Schema, Authorization and Base standards entries for `users.deleted_at`, the `User::delete()` obfuscation override and `UserPolicy::delete()`'s trashed-target refusal; indexed the fifth security page (`soft-delete-patterns.md`) and the sixth errors-log entry._

_Previously: 2026-08-13 — Task 0004 (Users list + create/edit backend): expanded the Overview, Authentication, Authorization, Routes, Base standards, Naming and Testing entries for the first permission-gated route, the first policy (`UserPolicy`), the `app/Policies/` folder, the administrator invitation path, and Livewire's `Index`-in-a-subfolder view-resolution exception; indexed the fourth security page (`livewire-authorization.md`) and the fifth errors-log entry._
