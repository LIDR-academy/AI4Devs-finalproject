[pytest]
testpaths = tests/backend
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: unit tests (isolated, no external I/O)
    e2e: end-to-end tests hitting real external services
    slow: long-running tests
addopts = -ra --tb=short
