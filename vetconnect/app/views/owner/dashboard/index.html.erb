<div class="px-4 sm:px-6 lg:px-8">
  <!-- Header -->
  <div class="animate-fade-in">
    <h1 class="text-3xl font-bold tracking-tight text-gray-900">¡Bienvenido, <%= current_user.first_name %>! 👋</h1>
    <p class="mt-2 text-lg text-gray-600">Gestiona la salud de tus mascotas en un solo lugar</p>
  </div>

  <!-- Stats -->
  <div class="mt-8 grid grid-cols-1 gap-5 sm:grid-cols-2 lg:grid-cols-3 animate-fade-in">
    <!-- Total Pets -->
    <div class="overflow-hidden rounded-xl bg-white shadow-sm ring-1 ring-gray-900/5 card-hover">
      <div class="p-6">
        <div class="flex items-center">
          <div class="flex-shrink-0">
            <div class="rounded-lg bg-purple-100 p-3">
              <span class="text-3xl">🐕</span>
            </div>
          </div>
          <div class="ml-5 w-0 flex-1">
            <dl>
              <dt class="text-sm font-medium text-gray-500 truncate">Mis Mascotas</dt>
              <dd class="flex items-baseline">
                <div class="text-3xl font-semibold text-gray-900"><%= @pets.count %></div>
                <%= link_to owner_pets_path, class: "ml-2 text-sm font-medium text-purple-600 hover:text-purple-500" do %>
                  Ver todas →
                <% end %>
              </dd>
            </dl>
          </div>
        </div>
      </div>
    </div>

    <!-- Upcoming Appointments -->
    <div class="overflow-hidden rounded-xl bg-white shadow-sm ring-1 ring-gray-900/5 card-hover">
      <div class="p-6">
        <div class="flex items-center">
          <div class="flex-shrink-0">
            <div class="rounded-lg bg-blue-100 p-3">
              <span class="text-3xl">📅</span>
            </div>
          </div>
          <div class="ml-5 w-0 flex-1">
            <dl>
              <dt class="text-sm font-medium text-gray-500 truncate">Próximas Citas</dt>
              <dd class="flex items-baseline">
                <div class="text-3xl font-semibold text-gray-900"><%= @upcoming_appointments.count %></div>
                <%= link_to owner_appointments_path, class: "ml-2 text-sm font-medium text-blue-600 hover:text-blue-500" do %>
                  Ver todas →
                <% end %>
              </dd>
            </dl>
          </div>
        </div>
      </div>
    </div>

    <!-- Quick Action -->
    <div class="overflow-hidden rounded-xl gradient-bg shadow-sm card-hover">
      <div class="p-6">
        <div class="text-white">
          <h3 class="text-lg font-semibold mb-2">¿Necesitas una cita?</h3>
          <p class="text-sm text-gray-100 mb-4">Agenda una consulta para tu mascota de forma rápida</p>
          <%= link_to new_appointment_path, class: "inline-flex items-center rounded-md bg-white px-4 py-2 text-sm font-semibold text-purple-600 shadow-sm hover:bg-gray-100 transition-colors" do %>
            <span class="mr-2">➕</span> Agendar Cita
          <% end %>
        </div>
      </div>
    </div>
  </div>

  <div class="mt-10 grid grid-cols-1 gap-6 lg:grid-cols-2">
    <!-- My Pets Card -->
    <div class="animate-fade-in overflow-hidden rounded-xl bg-white shadow-sm ring-1 ring-gray-900/5">
      <div class="border-b border-gray-200 bg-gray-50 px-6 py-4">
        <div class="flex items-center justify-between">
          <h2 class="text-lg font-semibold text-gray-900">🐾 Mis Mascotas</h2>
          <%= link_to new_pet_path, class: "text-sm font-medium text-purple-600 hover:text-purple-500" do %>
            + Registrar Nueva
          <% end %>
        </div>
      </div>
      <div class="p-6">
        <% if @pets.any? %>
          <ul class="divide-y divide-gray-100">
            <% @pets.each do |pet| %>
              <li class="py-4 hover:bg-gray-50 -mx-2 px-2 rounded-lg transition-colors">
                <div class="flex items-center justify-between">
                  <div class="flex items-center min-w-0 gap-x-4">
                    <div class="flex-shrink-0">
                      <div class="h-12 w-12 rounded-full bg-gradient-to-br from-purple-400 to-pink-400 flex items-center justify-center text-2xl">
                        <%= pet.species == 'dog' ? '🐕' : pet.species == 'cat' ? '🐈' : '🐾' %>
                      </div>
                    </div>
                    <div class="min-w-0 flex-auto">
                      <%= link_to owner_pet_path(pet), class: "text-sm font-semibold leading-6 text-gray-900 hover:text-purple-600" do %>
                        <%= pet.name %>
                      <% end %>
                      <p class="mt-1 text-xs leading-5 text-gray-500">
                        <%= pet.species.humanize %> • <%= pet.age %> años
                        <% if pet.breed.present? %>
                          • <%= pet.breed %>
                        <% end %>
                      </p>
                    </div>
                  </div>
                  <div class="flex-shrink-0">
                    <span class="inline-flex items-center rounded-full bg-purple-50 px-2.5 py-0.5 text-xs font-medium text-purple-700">
                      <%= pet.gender.humanize %>
                    </span>
                  </div>
                </div>
              </li>
            <% end %>
          </ul>
          <div class="mt-6">
            <%= link_to owner_pets_path, class: "block w-full rounded-lg border border-gray-300 bg-white px-4 py-2 text-center text-sm font-semibold text-gray-700 shadow-sm hover:bg-gray-50 transition-colors" do %>
              Ver todas mis mascotas (<%= @pets.count %>)
            <% end %>
          </div>
        <% else %>
          <div class="text-center py-12">
            <span class="text-6xl">🐾</span>
            <h3 class="mt-4 text-sm font-semibold text-gray-900">No tienes mascotas registradas</h3>
            <p class="mt-2 text-sm text-gray-500">Comienza registrando tu primera mascota para poder agendar citas.</p>
            <%= link_to new_pet_path, class: "mt-4 inline-flex items-center rounded-md gradient-bg px-4 py-2 text-sm font-semibold text-white shadow-sm hover:opacity-90 transition-opacity" do %>
              <span class="mr-2">➕</span> Registrar Mi Primera Mascota
            <% end %>
          </div>
        <% end %>
      </div>
    </div>

    <!-- Upcoming Appointments Card -->
    <div class="animate-fade-in overflow-hidden rounded-xl bg-white shadow-sm ring-1 ring-gray-900/5">
      <div class="border-b border-gray-200 bg-gray-50 px-6 py-4">
        <div class="flex items-center justify-between">
          <h2 class="text-lg font-semibold text-gray-900">📅 Próximas Citas</h2>
          <%= link_to owner_appointments_path, class: "text-sm font-medium text-purple-600 hover:text-purple-500" do %>
            Ver todas
          <% end %>
        </div>
      </div>
      <div class="p-6">
        <% if @upcoming_appointments.any? %>
          <ul class="divide-y divide-gray-100">
            <% @upcoming_appointments.each do |appointment| %>
              <li class="py-4 hover:bg-gray-50 -mx-2 px-2 rounded-lg transition-colors">
                <div class="flex items-center gap-x-3">
                  <div class="flex-shrink-0">
                    <div class="h-12 w-12 rounded-lg bg-blue-100 flex flex-col items-center justify-center">
                      <span class="text-xs font-semibold text-blue-600"><%= appointment.appointment_date.strftime('%b').upcase %></span>
                      <span class="text-lg font-bold text-blue-700"><%= appointment.appointment_date.day %></span>
                    </div>
                  </div>
                  <div class="min-w-0 flex-auto">
                    <p class="text-sm font-semibold leading-6 text-gray-900"><%= appointment.pet.name %></p>
                    <p class="mt-1 text-xs leading-5 text-gray-500">
                      <%= appointment.appointment_date.strftime('%H:%M') %> • Dr. <%= appointment.veterinarian.last_name %>
                    </p>
                    <p class="mt-1 text-xs text-gray-400"><%= truncate(appointment.reason, length: 40) %></p>
                  </div>
                  <div class="flex-shrink-0">
                    <span class="inline-flex items-center rounded-full status-<%= appointment.status %> px-2 py-1 text-xs font-medium">
                      <%= appointment.status.humanize %>
                    </span>
                  </div>
                </div>
              </li>
            <% end %>
          </ul>
          <div class="mt-6">
            <%= link_to owner_appointments_path, class: "block w-full rounded-lg border border-gray-300 bg-white px-4 py-2 text-center text-sm font-semibold text-gray-700 shadow-sm hover:bg-gray-50 transition-colors" do %>
              Ver todas las citas
            <% end %>
          </div>
        <% else %>
          <div class="text-center py-12">
            <span class="text-6xl">📅</span>
            <h3 class="mt-4 text-sm font-semibold text-gray-900">No tienes citas próximas</h3>
            <p class="mt-2 text-sm text-gray-500">Agenda una consulta veterinaria para el cuidado de tu mascota.</p>
            <%= link_to new_appointment_path, class: "mt-4 inline-flex items-center rounded-md gradient-bg px-4 py-2 text-sm font-semibold text-white shadow-sm hover:opacity-90 transition-opacity" do %>
              <span class="mr-2">➕</span> Agendar Primera Cita
            <% end %>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <!-- Quick Actions Bar -->
  <div class="mt-8 animate-fade-in">
    <div class="rounded-xl bg-gradient-to-r from-purple-50 to-pink-50 p-6 shadow-sm ring-1 ring-purple-100">
      <div class="flex flex-col sm:flex-row items-center justify-between gap-4">
        <div class="flex items-center">
          <span class="text-4xl mr-4">💡</span>
          <div>
            <h3 class="text-lg font-semibold text-gray-900">Acciones Rápidas</h3>
            <p class="text-sm text-gray-600">Accede rápidamente a las funciones más usadas</p>
          </div>
        </div>
        <div class="flex flex-wrap gap-2">
          <%= link_to new_pet_path, class: "inline-flex items-center rounded-md bg-white px-4 py-2 text-sm font-semibold text-gray-700 shadow-sm ring-1 ring-gray-300 hover:bg-gray-50 transition-colors" do %>
            🐕 Nueva Mascota
          <% end %>
          <%= link_to new_appointment_path, class: "inline-flex items-center rounded-md gradient-bg px-4 py-2 text-sm font-semibold text-white shadow-sm hover:opacity-90 transition-opacity" do %>
            📅 Agendar Cita
          <% end %>
          <%= link_to clinics_path, class: "inline-flex items-center rounded-md bg-white px-4 py-2 text-sm font-semibold text-gray-700 shadow-sm ring-1 ring-gray-300 hover:bg-gray-50 transition-colors" do %>
            🏥 Ver Clínicas
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
