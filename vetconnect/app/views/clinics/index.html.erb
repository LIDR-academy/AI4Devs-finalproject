<div class="px-4 sm:px-6 lg:px-8">
  <!-- Header -->
  <div class="sm:flex sm:items-center sm:justify-between animate-fade-in">
    <div>
      <h1 class="text-3xl font-bold tracking-tight text-gray-900">Clínicas 🏥</h1>
      <p class="mt-2 text-sm text-gray-600">
        Encuentra la clínica veterinaria más cercana
      </p>
    </div>
  </div>

  <!-- Clinics Grid -->
  <% if @clinics.any? %>
    <div class="mt-8 grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3 animate-fade-in">
      <% @clinics.each do |clinic| %>
        <div class="bg-white shadow-sm ring-1 ring-gray-900/5 rounded-xl overflow-hidden card-hover">
          <div class="p-6">
            <!-- Clinic Header -->
            <div class="flex items-start justify-between mb-4">
              <div class="flex items-center">
                <div class="flex-shrink-0">
                  <div class="h-12 w-12 rounded-lg bg-gradient-to-br from-blue-400 to-cyan-400 flex items-center justify-center text-2xl">
                    🏥
                  </div>
                </div>
                <div class="ml-3">
                  <h3 class="text-lg font-semibold text-gray-900"><%= clinic.name %></h3>
                  <% if clinic.active? %>
                    <span class="inline-flex items-center rounded-full bg-green-50 px-2 py-0.5 text-xs font-medium text-green-700">
                      Activa
                    </span>
                  <% else %>
                    <span class="inline-flex items-center rounded-full bg-gray-50 px-2 py-0.5 text-xs font-medium text-gray-700">
                      Inactiva
                    </span>
                  <% end %>
                </div>
              </div>
            </div>

            <!-- Clinic Details -->
            <div class="space-y-3 text-sm">
              <div class="flex items-start">
                <span class="mr-2">📍</span>
                <span class="text-gray-600"><%= clinic.address %></span>
              </div>
              
              <div class="flex items-center">
                <span class="mr-2">📞</span>
                <span class="text-gray-600"><%= clinic.phone %></span>
              </div>
              
              <div class="flex items-center">
                <span class="mr-2">✉️</span>
                <span class="text-gray-600"><%= clinic.email %></span>
              </div>

              <!-- Operating Hours Summary -->
              <% if clinic.operating_hours.present? %>
                <div class="pt-3 border-t border-gray-100">
                  <p class="text-xs font-medium text-gray-500 mb-2">Horarios:</p>
                  <% weekday_hours = clinic.operating_hours_for(:monday) %>
                  <% if weekday_hours %>
                    <p class="text-xs text-gray-600">
                      Lun-Vie: <%= weekday_hours[:open] %> - <%= weekday_hours[:close] %>
                    </p>
                  <% end %>
                  <% weekend_hours = clinic.operating_hours_for(:saturday) %>
                  <% if weekend_hours %>
                    <p class="text-xs text-gray-600">
                      Sábado: <%= weekend_hours[:open] %> - <%= weekend_hours[:close] %>
                    </p>
                  <% end %>
                </div>
              <% end %>
            </div>

            <!-- Actions -->
            <div class="mt-6 flex gap-2">
              <%= link_to clinic_path(clinic), class: "flex-1 text-center rounded-lg bg-purple-50 px-3 py-2 text-sm font-semibold text-purple-700 hover:bg-purple-100 transition-colors" do %>
                Ver Detalles
              <% end %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <!-- Empty State -->
    <div class="mt-12 text-center animate-fade-in">
      <div class="mx-auto h-24 w-24 text-7xl">🏥</div>
      <h3 class="mt-4 text-lg font-semibold text-gray-900">No hay clínicas disponibles</h3>
      <p class="mt-2 text-sm text-gray-500">
        Actualmente no hay clínicas registradas en el sistema.
      </p>
    </div>
  <% end %>

  <!-- Info Card -->
  <div class="mt-10 rounded-lg bg-blue-50 p-6 shadow-sm ring-1 ring-blue-100 animate-fade-in">
    <div class="flex">
      <span class="text-2xl mr-3">💡</span>
      <div>
        <h3 class="text-sm font-semibold text-blue-900">Información Importante</h3>
        <ul class="mt-2 text-sm text-blue-700 list-disc list-inside space-y-1">
          <li>Verifica los horarios de atención antes de agendar una cita</li>
          <li>Algunas clínicas pueden tener horarios especiales en días festivos</li>
          <li>Contacta directamente a la clínica para emergencias</li>
        </ul>
      </div>
    </div>
  </div>
</div>
