// Import and register all your controllers from the importmap under controllers/*
import { application } from "./application"

// Note: Currently no Stimulus controllers are defined in this application
// If you add Stimulus controllers later, uncomment one of the following:
//
// Option 1: Eager load all controllers (loads immediately)
// import { eagerLoadControllersFrom } from "@hotwired/stimulus-loading"
// eagerLoadControllersFrom("controllers", application)
//
// Option 2: Lazy load controllers (loads when they appear in DOM)
// import { lazyLoadControllersFrom } from "@hotwired/stimulus-loading"
// lazyLoadControllersFrom("controllers", application)
