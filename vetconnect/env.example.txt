# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=localhost

# Production Database
VETCONNECT_DATABASE_PASSWORD=change_me_in_production

# Rails Configuration
RAILS_ENV=development
RAILS_MASTER_KEY=generate_with_rails_credentials_edit

# Application Host
HOST=localhost:3000

# SMTP Configuration (for production email)
SMTP_ADDRESS=smtp.example.com
SMTP_PORT=587
SMTP_DOMAIN=vetconnect.com
SMTP_USERNAME=your_smtp_username
SMTP_PASSWORD=your_smtp_password

# Redis (for caching and ActionCable in production)
REDIS_URL=redis://localhost:6379/0

# Optional: OTP Secret for 2FA (future implementation)
# OTP_SECRET_ENCRYPTION_KEY=generate_with_SecureRandom_hex_32

# RENAME THIS FILE TO .env BEFORE USING
